# 交接文档 — 2026-09-22（L3 方向一 Phase 1：观测模型保真度）

> 目标（`docs/ROADMAP_L3.md` 方向一"多模态感知"的地基）：让 `sensor_model` 的
> `lidar_mode=1` 真点云模式**从物理上不可用变成可用**，为后续「雷达/相机模型 +
> 多模态置信加权融合 + sensor 切默认」扫清障碍。
>
> **本 Phase 不动默认编排**（`config/pipeline.json` 一字未改），因此行为零变化、
> 无需重生成 baseline。

---

## 0. 一句话总结

抽出 `modules/adas_nodes/lidar_scan.{h,c}` 纯逻辑（3D 多层扫描 / 容量守卫 / 可达距离
推导 / 自持 RNG），顺带修掉**四个叠加缺陷**：单层扫描 z 恒 0、写点无容量守卫（会越界写）、
测距噪声 σ=0.08·range（33m 处 2.6m，把相邻车道的两台车糊成 width=17m 的"车"）、
以及**消费者侧的第二份 60m 量程门**（生产者发到 120m 也被整帧丢掉）。

实测（`urban_challenge` 45s，sensor 模式）：

| 指标 | 改前 | 改后 |
|---|---|---|
| 锥内识别率（真值限定在传感器 120°/120m 锥内） | 25.6% | **86.9%** |
| 每帧检出目标数 | 0.76 | **3.58** |
| 粘连大簇（width > 4m） | 21/25 | **0/118** |
| `perception_coverage`（官方口径） | 0.742 | **1.000** |
| `recognition_rate_overall`（官方口径） | 0.199 | 0.291 |

官方口径的 `recognition_rate` 仍 < 50% FAIL —— 但**这不是感知的问题，是度量的分母
问题**（见 §3.1）：只有约 27% 的真值实体落在前向 120°/120m 锥内，任何真实前向传感器
在该场景的上限就是 ~30%。

---

## 1. 改了什么（文件级）

| 文件 | 改动 |
|---|---|
| `modules/adas_nodes/lidar_scan.h` | **新增**。`LidarScanPlan`（射线数/仰角层/FOV/量程/安装高度/噪声/丢失）、`LidarScanTarget`、`lidar_scan_generate()`、`lidar_scan_detectable_range()`、`lidar_scan_plan_sanitize()`、`lidar_scan_height_from_width()`、自持 xorshift64* RNG |
| `modules/adas_nodes/lidar_scan.c` | **新增**。3D 射线 vs 3D AABB（slab）、外接球预筛、容量守卫、距离噪声、可达距离数值推导 |
| `modules/adas_nodes/sensor_model_node.c` | 删除内联的 `rand_uniform_01` / `rand_gauss` / `ray_aabb_t` / 射线循环（同 commit 物理删除），`publish_raycast_points` 改为"g 状态 → plan/targets → lidar_scan_generate → 序列化"；新增 6 个参数（`lidar_azimuth_rays` / `lidar_channels` / `lidar_vfov_min_deg` / `lidar_vfov_max_deg` / `lidar_height_m` / `lidar_noise_rel` / `lidar_scan_seed`）；`lidar_noise_rel` 独立于 `obs_noise_std_m` |
| `modules/adas_nodes/CMakeLists.txt` | `sensor_model` 目标加 `lidar_scan.c` |
| `CMakeLists.txt` | `test_adas_nodes_logic` 加 `modules/adas_nodes/lidar_scan.c` |
| `tests/test_adas_nodes_logic.c` | +8 用例（FOV 边界 / 量程门 / **容量守卫** / 确定性 / 3D z 非零 + 单层平面 / 可达距离单调 / plan 夹紧 / 高度启发式 + 量程契约防漂移）；54/54 |
| `config/pipeline_sensor.json` | **新增**。`pipeline.json` 的 sensor 变体：`sensor_model.lidar_mode=1` + 扫描参数，`perception.mode=sensor` + **同值 `lidar_max_range_m`** + `dbscan_eps=1.2` |

**参数默认值**（`config/pipeline_sensor.json`）：量程 120m、方位 720 根（0.167°/根）、
仰角 32 层（22° 跨度 → 0.71°/层）、安装高 1.8m、`lidar_noise_rel=0.002`。

> 选型依据写在 `lidar_scan.h` 的注释与 `lidar_scan_detectable_range()` 里：车（1.5m）
> 在 120m 处的仰角张角约 0.7°，所以层间距必须 ≤0.71° —— 原来的 8 层（3.14°）会从层缝里
> 漏掉远处目标；行人（0.5m）在 30m 处方位张角 0.95°，方位间距需 ≤0.24°。

---

## 2. 四个叠加缺陷（含证据）

### 2.1 单层水平扫描：点云是一张平面（z 恒 0）

旧 `publish_raycast_points` 只发射方位射线、`p.z = 0.0f` 写死，AABB 求交也是 2D
（忽略 z）。后果：`ClusterBounds.min_z/max_z` 恒 0 → 无法做地面分割、无法用高度做
3D 分类；且**装在 1.8m 高处的水平射线物理上看不见 1.5m 高的车**。本次改为
"方位 × 仰角"的 3D 射线 + 3D AABB，点云有真实 z。

> 副作用（有意为之）：`channels=1` 现在语义是"单束在传感器水平面"，只会扫到高于安装面
> 的目标 —— 这是物理正确的结果，单测里用 3.0m 高的目标把"方位角几何"与"仰角可见性"
> 解耦后验证。

### 2.2 写点循环没有容量守卫（会越界写内存）

旧实现 `LidarPoint* p = &cloud.points[cloud.count]; ... cloud.count++;` **没有任何上限
检查**。`n_rays=240` < 容量 2048 时侥幸没炸；一旦提高射线数（尤其 42 个 actor 的
`lane_change_traffic`），命中数可以超过 2048 → 越界写栈/静态缓冲。本次在
`lidar_scan_generate` 里写满即停并把 `dropped` 上报（节点侧限频 WARN）。
单测 `test_lidar_scan_capacity_guard` 用 2048×32 根射线罩住整个 FOV 钉死该行为。

### 2.3 测距噪声模型错误：σ = 0.08·range（33m 处 2.6m）

旧代码注释写的是"σ=α·range"，直接把 `obs_noise_std_m`（0.08）当相对系数用。真实 LiDAR
测距噪声是 2~5cm 量级；0.08·range 在 33m 处就是 **2.6m**，而相邻车道中心间距只有 3.5m
（车宽 2m → 车间空隙 1.5m）—— 噪声足以把两台车糊成一个簇。实测证据（sensor 模式）：

```
ego=(169.7,-5.3)  真值：car id=1 (202.5,-1.75) / car id=2 (207.0,-5.25)
感知：1 个簇 (202.1,-3.07) width=17.12m  ← 两台车糊成一个
```

改为 `σ = noise_std_m + noise_rel·range`（默认 0.08m + 0.2%/m；120m 处 0.32m），
`lidar_noise_rel` 单列参数，`obs_noise_std_m` 回归其字面语义（绝对 1σ，与 mode=0 一致）。

### 2.4 ⚠️ 最致命：消费者侧有第二份量程门（默认 60m）

`perception_node.cpp:495` 有**自己的** `lidar_max_range_m = 60.0`（可被同名 param 覆盖），
`perception_points_from_cloud()` 用它把远处点全部丢弃。只在 `sensor_model` 上把量程提到
120m 时，**生产者发到 120m、消费者把 >60m 的点静默丢掉**，无任何日志。
修好后锥内识别率从 37.0%（噪声修完时）跳到 86.9% —— 这一步的收益最大。

这正是 CLAUDE.md 铁律里那条 **"同一功能出现第二份实现 = 违规，必须收敛到权威模块"**：
量程目前是"生产者参数 + 消费者参数"，两者必须同值，且**分歧时毫无信号**。

### 2.5 DBSCAN `eps=2.0` > 相邻车道空隙 1.5m

即使点云零噪声，`eps=2.0m` 也会把相邻车道的两台车连通。本次在 sensor 配置里把
`dbscan_eps` 收到 **1.2**（`dbscan_eps` 本身是可配 param，`perception_node.cpp:514`）。
配合 0.167° 方位分辨率（120m 处点距 0.35m < 1.2m）不牺牲连通性。

---

## 3. 两个度量伪影（**阻塞 Phase 2**，本次只量化未改）

### 3.1 识别率的分母不感知 FOV/量程

`demo_evaluator._compute_perception_metrics()` 对**所有**真值实体求分母，不筛选
"sensor 能不能看到"。实测该场景（33 帧）：

```
真值实体 9.36/帧，其中落在 ego 前向 120° FOV + 120m 量程内的只有 2.55/帧 = 27.2%
```

于是**任何真实前向传感器**的 evaluator 口径上限就是 ~27%。实测印证：
`recognition_rate_overall = 0.291` ≈ 锥内 86.9% × 27.2% + 噪声。

`ground_truth` 模式能拿 1.000，只因它把**全部**真值（含背后、含 800m 外的 actor）直通，
根本不存在"可见性"概念 —— 也就是说这个指标对 sensor 模式**结构性不公平**。

**要落地 sensor 门禁，必须先让分母锥内化**（按 ego 位姿 + FOV + 量程筛真值，或改用
"tracker 已确认航迹 vs 真值"的一致口径）。这是一次**度量语义变更**，按 2026-09-21b §14.3
的教训（"先量化伪影、再改门禁，顺序不能反"）应单独决策。

### 3.2 sensor 模式的簇 id 每帧都变 → 预警提前量恒 0

`perception_points.c::perception_clusters_to_obstacles()` 用
`ob->id = pose->frame_id * 100u + ci` 当障碍物 id → **每帧都是新 id**。预警提前量指标
（按 id 跨帧跟踪）因此恒为 0，实测 `warning lead time too short: min=0.00s (11 critical events)`。

上一 session（§14.1）在 ground_truth 模式用 `vehicle/state` 的 `oid%d` 修掉了这一点，
但**sensor 模式没有 oid 可用**。正确修法是让 sensor 模式也用跨帧稳定 id
（消费 `object_tracker` 的已确认航迹 id，或给簇做最近邻跨帧关联）。

---

## 4. 验证状态

| 项 | 结果 |
|---|---|
| 顶层构建 / adas_nodes 构建 | ✅ 通过（`lidar_scan.c`、`sensor_model_node.c` 零 warning） |
| `test_adas_nodes_logic` | ✅ **54/54**（新增 8 例） |
| 五个 gate（topic / msg-layout / plugin-symbol / zombie） | ✅ 全 OK（topic contract 6 configs，含新增 `pipeline_sensor.json`） |
| `scenarioctl validate` | ✅ PASS |
| `scenario_regression --baseline`（默认编排） | 见 §4.1 |
| sensor 模式端到端（`urban_challenge` 45s） | ✅ `perception_coverage = 1.000`、`perceived_count_avg = 2.740`；锥内识别率 86.9% |
| sensor 模式行为门禁 | ❌ 仍 FAIL 3 项：识别率（§3.1 分母伪影）、预警提前量（§3.2 id 伪影）、progress（真值对照组同样 FAIL，非本次引入） |

### 4.1 默认编排零变化（本次是纯加法）

`lidar_mode=0` 时所有新代码 inert：新参数只有 `sensor_model` 在 mode=1 时读；
`config/pipeline.json` 的 `lidar_max_range_m` 仍是 60（mode=0 的可见点估计也读它，故不动）。
实测 `scenario_regression --baseline` 结果见本文件末尾补记。

---

## 5. 未解决 / 下一步

| 优先级 | 项 | 说明 |
|---|---|---|
| ~~P0~~ ✅ | ~~识别率分母锥内化（§3.1）~~ | 已在 §9 落地（**仅 sensor 模式武装**，见 §9 的踩坑记录） |
| ~~P1~~ ✅ | ~~sensor 模式稳定 id（§3.2）~~ | 已在 §12 结案（改按真值身份跟踪） |
| ~~P1~~ ✅ | ~~量程门收敛成单一事实源~~ | 已在 §10 落地为 `ci/gates/sensor_wiring_check.py`（`sensor-wiring-gate`） |
| P1 | sensor 模式行为回归 | 已在 §10 补测 `lane_change_traffic`（锥内识别率 92.9%、零告警） |
| ~~P1~~ | ~~`DBSCAN_MAX_CLUSTERS=32` 在 42 actor 场景成瓶颈~~ | **§10 实测推翻**：该场景 actor 间隔 200m，同帧锥内最多 2 个，簇数上限从未接近 |
| P2 | **同一目标被拆成多个簇**（观测模型侧，根因已定位） | 见 §13：车体轴对齐 AABB（不含朝向）导致斜视时"正面 + 近侧面"两族点，近侧面采样过稀被 DBSCAN 切成独立小簇。碎片**直达 planning / behavior_planner / safety_control**（它们都直接订阅 `perception/obstacles`），影响真实但尚未量化 |
| P2 | 单帧 2048 点容量 | 想模拟更真实的扫描密度（如 64 层 × 0.1°）必须扩 `LidarPointCloud.points[]`，会动 ABI（slam / msg gate / 回放），需单独决策 |
| P2 | 障碍物朝向 / 高度 | `vehicle/state` 不发 `oh%d` 与 heading，当前用 `ow` 启发式 + AABB 与 ego 同向，横穿/掉头目标仍失真 |
| P2 | 雷达传感器模型 / 相机模型 / 置信加权融合 | 方向一后续；先让第一模态可信（本 Phase 已完成这步） |

---

## 6. 不要做的事

1. **不要**只改 `sensor_model.lidar_max_range_m` 而不改 `perception.lidar_max_range_m` ——
   消费者的量程门会把远处的点静默丢掉，现象是"改了没效果"（§2.4）
2. **不要**把测距噪声写成 `σ = k·range` 且 k 取 `obs_noise_std_m`（0.08）—— 33m 处 2.6m，
   相邻车道的车会糊成一个宽簇（§2.3）
3. **不要**在 sensor 模式下用 `dbscan_eps >= 1.5`（相邻车道中心间距 3.5m、车宽 2m）
4. **不要**在 `sensor/lidar_points` 上发 JSON 或别的类型（二进制契约是 `LidarPointCloud`）
5. **不要**用 `msg_init_typed(&struct, sizeof(struct))` 发 `LidarPointCloud`（见
   `HANDOFF_2026-09-21b.md` §3.2 的 msg_codegen padding 陷阱）
6. **不要**在 sensor 模式给 DBSCAN 开 RANSAC 地面去除（本模型不投地面回波，z 不是地面）
7. **不要**相信手写的 `docs/PIPELINE_ARCHITECTURE.md`（DEPRECATED），真拓扑看
   `config/pipeline.json` 的 `processes[]`

---

## 7. 复现命令

```bash
# 构建 + 单测
cmake --build build -j$(nproc)
cmake --build build/modules/adas_nodes -j$(nproc)
env -u LD_LIBRARY_PATH ./build/bin/test_adas_nodes_logic        # 54/54

# 门禁
for g in topic_contract_check msg_layout_check plugin_symbol_check zombie_ban_check; do
  python3 ci/gates/$g.py; done
python3 tools/scenarioctl.py validate

# 默认编排零回归（本 Phase 是纯加法）
python3 ci/evaluators/scenario_regression.py --workers 1 --baseline

# sensor 模式端到端（FLOW_PIPELINE 让 demo.sh / evaluator 都读该配置）
FLOW_PIPELINE=config/pipeline_sensor.json python3 ci/evaluators/demo_evaluator.py \
  --duration 45 --interval 0.5 --scenario scenarios/urban_challenge.json
```

---

## 8. 一句话给下一位接手的人

观测模型本身现在是可信的（锥内识别率 86.9%、零粘连簇、有容量守卫、有 8 条单测），
**真正卡住 sensor 模式上车的是度量**：识别率分母不筛 FOV/量程（前向传感器上限 ~30%）、
sensor 模式簇 id 每帧变（预警提前量恒 0）。先把这两条度量伪影修掉，再谈"切默认 + 进 CI 门禁"。
另外那条"生产者 120m / 消费者 60m"的双量程门值得变成一条 gate —— 它静默、致命、且属于
仓库明令禁止的"同一功能两份实现"。

---

## 9. 追加（同日续）：识别率分母锥内化 ✅

§3.1 量化的那条度量伪影已修。改动面比预期小：**只动 `_compute_perception_metrics`
的真值枚举一处**，门禁判定 / 报告打印 / `--json-out` 都自动跟随 summary。

| 文件 | 改动 |
|---|---|
| `ci/evaluators/demo_evaluator.py` | 新增 `_node_params()` / `pipeline_perception_spec()`（返回 `(是否 sensor 模式, FOV, 量程)`，量程取生产者/消费者两处的 **min**）；`_compute_perception_metrics` 的 truth 枚举加锥内过滤 + 5 个新返回键；`score()` 在"sensor 模式但锥内化未生效"时打 WARN；两条识别率失败串改说 `observable truth samples` |
| `ci/evaluators/test_demo_evaluator.py` | +4 用例（锥外真值不计入分母 / **锥只在 sensor 模式武装** / 缺 ego 位姿退回旧口径并标记未生效 / 量程取 min），27/27 |

关键设计：
- **规格来自在跑的 config**，不写死常量 —— 锥内化的分母必须等于"这份 pipeline 实际能
  看到的范围"，否则改了 config 指标就失真。
- **锥只在 `perception.mode == "sensor"` 时武装**（`pipeline_perception_spec()`）。
  这一点是踩过坑才定下来的：第一版无条件锥内化，`scenario_regression --baseline`
  立刻冒出 2 个新 FAIL —— `city_comprehensive` / `multi_light` 只声明 1~2 辆车且**全程
  在视锥外**，锥内化后分母归零 → `INCONCLUSIVE [recognition_rate_vehicle]: cannot
  evaluate`，门禁从"虚满分"翻成"无法判定"，**两者都是错的**。正确口径是
  "分母 = 这份 pipeline 的**感知输入级**能知道什么"：sensor 模式吃点云、有 FOV/量程；
  ground_truth 模式直接吃 `vehicle/state` 真值、能"看到"的就是全部真值 → 不筛。
- **sensor 模式下缺 ego `y`/`heading` 时退回"全部真值"旧口径，但把
  `perception_observability_applied=False` 报出来并由 `score()` 转 WARN** —— 不允许静默
  退回旧口径（否则度量退化无人知晓，这正是本项目反复踩的坑）。老 series / 单测构造的
  样本没有这两个键，所以现有 3 条识别率用例的断言原样成立。
- 量程取 **min**：生产者/消费者各有一份量程门，链路实际能看到的是较小者。

实测（`urban_challenge` 45s，sensor 模式；数值随 flowsim 非确定性有小幅波动，两次跑分别为
0.958/0.875 与 0.931/0.944）：

| 指标 | 改前 | 改后 |
|---|---|---|
| `recognition_rate_vehicle` | 0.302 FAIL | **0.93 ~ 0.96** |
| `recognition_rate_vru` | 0.250 FAIL | **0.88 ~ 0.94** |
| `recognition_rate_overall` | 0.291 | **0.93 ~ 0.94** |
| 分母样本数（vehicle/vru） | 672 / 192（全部真值） | 217 / 54（**锥内**真值） |

`ground_truth` 对照组仍 `1.000`（它不做锥内化，分母就是全部真值）—— 注意两种模式的
分母语义**本就不同**（各自描述"该编排的感知输入级能知道什么"），所以不应横向比较绝对值，
但**每种模式内部现在都是公平的**：sensor 模式再也不会因为"看不到物理不可见的东西"被判漏检。

**sensor 模式剩下的唯一真 FAIL 是 §3.2（簇 id 每帧变 → 预警提前量恒 0）**；另一条
"vehicle stuck or no progress" 在真值对照组同样 FAIL（288m < 400m），是场景阈值问题，
非本次引入。

> ⚠️ **`tests/baseline/*.json` 里的 `recognition_rate_*` / `truth_count_*` 现在是过期值**
> （分母口径变了）。本次**故意没有** `--update-baseline`：工作区里还有别人未提交的
> planning 修复，重生成会把未提交行为烤进 baseline。等 planning 改动落地后一起重生成。

## 10. 追加（同日续）：sensor 接线一致性 gate ✅ + 密集场景实测（含推翻一条旧判断）

### 10.1 新 gate：`ci/gates/sensor_wiring_check.py`（`sensor-wiring-gate`）

把三处分散在两个节点、任一处不一致就**静默**失效的配置钉住：

| 不变式 | 不一致的后果 |
|---|---|
| `perception.mode=="sensor"` ⇒ `sensor_model.lidar_mode==1` | 生产者不发点云，感知永远等不到（只在日志刷 "no fresh sensor/lidar_points"） |
| `perception.lidar_max_range_m` **不得小于** `sensor_model` 的 | 消费者把远处点静默全丢（实测锥内识别率 86.9%→37.0%） |
| 两个量程键必须**显式声明** | 依赖代码默认值（都是 60.0）时，"生产者改了消费者没改"在静态检查里发现不了 |

- 必须定位到 `processes[].name=="perception"` 的 `params.mode`，**不能全局 grep "mode"**
  （每个 config 的 `scheduler` 也有 `"mode": "choreo"`）。
- 已接进 `.github/workflows/ci.yml`（独立 job，与其它静态 gate 并行、无 `needs`）；
  登记进 `CLAUDE.md` 的治理表与 gate 清单。
- 当前仓库 6 个 config 全 PASS（唯一命中 sensor 模式的是 `pipeline_sensor.json`）。
- **漂移注入自测**（照 `msg_layout_check` 的手工注入惯例）：三种漂移各 exit 1 且报错点名 ——
  ①`lidar_mode=0` ②消费者量程 60 < 生产者 120 ③两处量程都不写。

### 10.2 ⚠️ 推翻 §5.1 的一条判断：`lane_change_traffic` 不会撞簇数上限

实测该场景（45 帧样本，sensor 模式）：

```
truth 42.00/帧（全部 actor 都在 scene 里）  但同帧锥内真值只有 0.93/帧（2.2%），最多 2 个
perceived 1.16/帧，单帧最多 3       (DBSCAN_MAX_CLUSTERS = 32 → 从未接近)
锥内真值距离 min=8.6m p10=17.6m     (→ 单帧 2048 点预算从未顶；零 "lidar cloud full")
锥内识别率 92.9%；perception 零告警（无 "no fresh" / "DBSCAN overrun" / deserialize failed）
perceived 宽度 median=1.09m，>4m 的占 12/52（超车时邻车道两车偶有粘连，epsilon=1.2 边界）
```

原因：actor 是"两车道各 200m 等间隔、互相错位 100m"，而量程/视锥只有 120m/120° ——
**42 个 actor 里同时进视锥的最多 2 个**。所以 §5.1 那句"42 actor 撞 `DBSCAN_MAX_CLUSTERS=32`"
是按 actor **总数**而非"同时可见数"的推断，**是错的**。

要真正压这两个上限，需要的是"很多目标**同时**在视锥内"的场景（同车道近距离排队 /
密集交叉口），而不是"actor 总数大"。这是后续造难例场景的依据。

## 11. 本批验证状态

| 项 | 结果 |
|---|---|
| `test_demo_evaluator.py` | ✅ 26/26（新增 3 例） |
| `test_evaluator_gate.py`（门禁自检） | ✅ 29/29（分母改动未让门禁失去判断力） |
| 五个 gate（topic / msg-layout / plugin-symbol / zombie / **sensor-wiring**） | ✅ 全 OK |
| ctest 顶层 / adas_nodes | ✅ 30/30 / 7/7 |
| `scenario_regression --baseline` | ✅ 8/8（`regress` 列全 `-`；识别率不在 tolerance 键里，故口径变化不报回归） |
| sensor 模式（urban_challenge 45s） | ✅ 识别率 FAIL 消失（0.958 / 0.875） |
| sensor 模式（lane_change_traffic 30s） | ✅ 锥内识别率 92.9%、零告警、零容量丢弃 |

**遗留**：§3.2（sensor 模式簇 id 每帧变 → 预警提前量恒 0）仍是 sensor 模式唯一真 FAIL；
`tests/baseline/*.json` 的识别率相关值待 planning 改动落地后统一重生成（§9 末尾）。

## 12. 追加（同日续）：预警提前量改按真值身份跟踪 ✅（§3.2 结案）

§3.2 修掉了。**两次换源才定下来**，过程值得记：

| 方案 | 结果 |
|---|---|
| 感知输出自己的 id（原状） | `perception/obstacles` 的 id 是帧内局部的（`perception_points.c` 的 `frame_id*100+ci`），每帧都变 → 提前量恒 0 且**凭空伪造** 11~16 个 critical event |
| 换成 `object_tracker` 的航迹 id | 提前量**均值**变成 6.66s（有救了），但 `min` 仍是 **0.00s** → 仍 FAIL。抓现场数据（73 帧样本）查因：12 个航迹 id 对应 9 个 actor，且两个"车"在 **14m / 16m** 处才首次出现（首次检测与临界同帧 → lead=0）。即这条路继承了**跟踪器的 id churn** |
| **按真值实体身份跟踪**（最终） | 仿真的真值 id 天然稳定；"检测到" = 本帧有感知输出落在该真值 `PERCEPTION_MATCH_DIST_M` 内；TTC 用该真值的前向距离 |

实测（`urban_challenge` 45s，sensor 模式）：

| 指标 | 改前 | 改后 |
|---|---|---|
| `warning_lead_min_s` | **0.00**（FAIL < 0.3） | **4.51** |
| `warning_lead_avg_s` | 0.00 | **14.16** |
| `critical_event_count` | 11~16（伪造） | **4**（真实事件） |
| `perceived_track_count` | 14（帧内 id 数） | **6**（被检测到的真值障碍数） |

sensor 模式现在**没有感知/预警类 FAIL 了**，只剩 `vehicle stuck or no progress`
（288m < 400m）—— 那条在真值对照组同样 FAIL（284m），是场景阈值/行为问题，与本次改动无关。

语义变化（有意，已写进 docstring 与单测）：
- 跟踪键从"某个内部 id"改成"障碍本身"，所以**幻影感知不再能造出 critical event**——
  本指标按真值定义障碍；假阳性导致的幽灵刹车属于**正精度**指标范畴，需要单独的门禁。
- 反之，真值障碍变成临界却**从未被命中** → 提前量 0（"完全没预警"），要 FAIL。
- 不依赖 `object_tracker` 是否存在了（原先为了拿稳定 id 必须依赖它），
  所以 `pipeline_manual.json` / `pipeline_car.json`（无跟踪器）也能算这一路。

单测 +4 例（真值身份跟踪 / 真值未命中 → lead 0 / 幻影不造事件 / 无临界事件），29/29。

> ⚠️ **§12 末尾线索的更正（同日复查）**：我最初把"两个车在 14/16m 才首次出现"
> 记成"可能是感知幻影（假阳性）"。**复查否掉了这个框架**，实测是：
>
> ```
> t=…447.1  近距真值车(前 40m): [(1, 225.2, -1.8)]        ← 只有一辆车
>           原始感知 perceived(4): id=55600(224.0,-2.0,w=2.4)
>                                 id=55601(226.9,-3.0,w=2.5)  ← 同车被拆成两个簇
>           航迹:                  id=9(224.4,-2.2), id=29(227.2,-3.3)  ← 遂成两条航迹
> ```
>
> - 真值侧**无瞬移**：`tp_cycle` 全为 0、无位置跳变；那台车是从 30m 一路以
>   ~10m/s 相对速度逼近到 8m，位移与时间自洽。它**早就在跟踪里**（`TRACK 9`）。
> - 所以既不是"凭空冒出的假障碍"，也不是仿真回收瞬移，而是**同一目标在
>   观测模型/聚类侧被拆成多个簇**（该侧属"仿真感应"范畴）。
> - **与 `vehicle stuck` 无关**：`ground_truth` 直通模式（无聚类、无分裂）下
>   该 FAIL 同样复现（284m）。这条联想一并撤回。
>
> 结论：待查项从"加正精度门禁抓幻影"改为"查清 10-15m 处簇分裂的成因"
> （观测模型的车体轴 AABB 不含朝上 / `dbscan_eps=1.2` 在该距离的分裂），
> 且优先级下调（不影响既有门禁结论：提前量指标已改按真值身份跟踪，不吃簇 id）。

---

## 13. 追加（同日续）：簇分裂根因已定位（离线探针，不起仿真）

用离线探针（链接真实的 `lidar_scan.c` + `perception_points.c` + `dbscan_cluster.c`，
单目标车 4.6×2.0×1.5，扫距离 8~30m × 方位角 0~30°）复现并定位了 §12 的簇分裂。

### 13.1 复现结果

| 距离/方位 | 簇数 | 形态 |
|---|---|---|
| 0°（正前）8~30m | **1** | 正常：x-extent ≈ 0.6（径向）、y-extent ≈ 2.0（车宽） |
| 10°/20°/30° + **8~15m** | **2~3** | 主簇（正面，n≈2000）+ 一个**细长条**：`y-extent ≈ 0.02~0.28m`、x 跨度 2.2~2.7m |
| 10°/20°/30° + 20~30m | 1 | 已合并（x-extent ≈ 4.9 = 整车长） |

细长条逐点打出来后确认它就是**车的近侧面**（y 恒定 = `y_min`，x 是沿车长的方向）。
典型：8m/10° 时

```
cluster[0] n=2001  x[4.46,6.77] y[0.26,2.34]   ← 正面（x≈5.4 一片）
cluster[1] n=47    x[7.44,9.79] y[0.38,0.40]   ← 近侧面（y 恒 0.39，48 = 8 条方位射线 × 6 层仰角）
```

### 13.2 根因

**观测模型的 AABB 在传感器系里轴对齐（不随车体朝向旋转）**，所以斜视一台车时它呈现
**两个可见面**：正面 + 近侧面。近侧面只在很窄的方位窗口里被采样
（此处 `az ∈ [atan(y_min/x_max), atan(y_min/x_min)]` ≈ 2.2°~4.0°，仅 ~11 条射线），
而且**仰角层被盒子的 z-slab 裁掉**（陡的射线在到达侧面之前就已经离开 z 区间）→
侧面只拿到 ~47 点，正面拿到 ~2000 点。DBSCAN 的"核心点"规则（`min_pts=4` within `eps`）
在疏密交界处断链 → 劈成两个簇。

排除项：
- **不是 eps 问题**：`eps=1.2` 与 `eps=2.0` 都会裂（2.0 只把侧面的一部分并进主簇）。
- **不是跟踪器 churn、也不是真值瞬移**（§12 已证）。
- **不是仰角维度**：DBSCAN 的邻域距离是**纯 2D**（`dx²+dy² ≤ eps²`，忽略 z）。

### 13.3 顺带纠一个我自己的错：探针一开始被 RANSAC 污染

首版探针没设地面去除模式，而 `dbscan_init` 默认 `GROUND_REMOVE_RANSAC`
（`perception_node` 在 sensor 模式显式改 `GROUND_REMOVE_NONE`）。于是 30m 处的
单台车被 RANSAC 清成 **0 簇**，我差点得出"30m 检不到"的错误结论。
改成 `GROUND_REMOVE_NONE` 后各距离都能检出。**再次印证 §3.3 的 RANSAC 陷阱**——
写任何离线点云实验都必须先对齐这一项。

### 13.4 影响面

`perception/obstacles`（**带碎片的原始输出**）的直接订阅者：
`object_tracker`、**`behavior_planner`**、**`planning`**、**`safety_control`**、
`inference`、`data_recorder`、`learner`、`monitor`。
即碎片不只是观感问题，**会进 planning 的 Frenet/TTC 代价与 safety_control 的 TTC**。
是否可测量地劣化驾驶行为尚未做 A/B（需"合并碎片 vs 不合并"对照）。

### 13.5 修复选项（**我已撤回最初的推荐**）

> ⚠️ 我最初推荐"(a) 给目标盒加朝向"。**这个推荐是错的，已撤回**：探针里
> ego 朝向 = 0、目标朝向 = 0（压根没设朝向），也就是**盒子和车本来就是对齐的
> （α=0），它照样裂**；而 `urban_challenge` 的 actor 全是沿 +x 直行
> （heading 0/π，α 同样是 0）。所以"加朝向"对本 bug 是 **no-op**。
> 真正的成因是：**斜视角下一个盒子必然露出两个面**，掠射的那个面在 2048 点预算下
> 采样过稀（本例 47 点 vs 正面 2000 点），被 DBSCAN 的核心点规则切断。
> 也就是说这是"盒子表示 + 点数预算"的联合产物，不是朝向失配。

| 方案 | 内容 | 代价 / 风险 |
|---|---|---|
| ~~(a) 给盒加朝向~~ | ~~`vehicle/state` 发 `oh%d` + 有向盒~~ | **已证伪**：α=0 时照样裂（见上） |
| (b) 碎片合并 | `perception_points.c` 里把间距 < ~1.0m **且合并后尺寸仍是一辆车的footprint** 的簇并成一个 | 可单测；但相邻车道空隙只有 1.5m，阈值余量窄，需要"合并后尺寸合理"做第二道闸 |
| (c) 提高点数预算 | 扩 `LidarPointCloud.points[]`（现 2048）让掠射面也够密 | 动 ABI（slam / msg gate / 回放），需单独决策 |
| (d) 记录 + 回归测试 | 按仓库惯例把该故障模式转成测试 | 零风险，但不解决 |

**当前处置：暂不做 (b)**，理由是——sensor 模式在全场景已 PASS（见 §14），碎片对
门禁结论无影响，而 (b) 的阈值余量窄、在密集车流里有误并风险。等有 A/B 证据
（合并 vs 不合并，在 dense_npc / lane_change_traffic 上比行为指标）再决定。

---

## 14. 追加（同日续）：sensor 模式全场景 PASS + CI 门禁落地

### 14.1 里程碑级结果：sensor 编排 8/8 PASS

`FLOW_PIPELINE=config/pipeline_sensor.json python3 ci/evaluators/scenario_regression.py --workers 1`
（跑完整 8 个 enabled 场景）：**全部 PASS**。

也就是说「真点云 → DBSCAN → planning/safety」这条链，现在与 `ground_truth` 直通
编排一样能过全部行为/感知门禁。**Phase 1 的前半句目标（"让 sensor 模式可信到能切默认"）
达成。**

⚠️ **顺带纠正我自己上一轮的一个假警报**：我先前几轮都报 "vehicle stuck or no progress:
x_delta 不到 400m"，并把它当成"两种编排都过不了的既有限制"。**那是我自己造出来的**——
我用 `--duration 40/45` 覆盖了场景时长，而 `scenarios/suite.json` 给 `urban_challenge`
的是 **60s**、判据 `min_distance_m=400`。按 60s 跑：

| 编排 | x_delta | recognition(vehicle/vru) | warning_lead_min | 结论 |
|---|---|---|---|---|
| ground_truth（默认） | 522 m | 1.000 / 1.000 | 13.03 s | **PASS** |
| **sensor（真点云）** | 490 m | 0.960 / 0.896 | 3.01 s | **PASS** |

**教训（对所有后续评测都适用）：跑评测必须用场景声明的时长**（或直接抄
`scenario_regression.py` 的命令），自己随手给 `--duration` 会得出假 FAIL。

### 14.2 CI 门禁落地（`.github/workflows/ci.yml` 的 `long-tests` job）

在既有的"默认编排 regression vs baseline"之后，加了**传感器编排回归**：

```yaml
      - name: Sensor-mode regression (real point cloud chain)
        run: |
          for s in urban_challenge dense_npc lane_change_traffic; do
            echo "─── sensor mode: $s ───"
            FLOW_PIPELINE=config/pipeline_sensor.json \
              python3 ci/evaluators/scenario_regression.py --workers 1 --only "$s"
          done
```

- 三个代表场景：`urban_challenge`（车+行人+信号灯）/ `dense_npc`（多车）/
  `lane_change_traffic`（42 actor、变道超车）；时长沿用 suite 条目（60/45/45s）。
- **不传 `--baseline`**：sensor 编排与 ground_truth 基线的行为差异不该被当成回归，
  这条只断言"每个场景自身的行为/感知门禁 PASS"（任一 FAIL → runner `return 2` → job 红）。
- 本地按仓库纪律照抄 `run:` 行复现：**3/3 PASS，EXIT=0**，仅两条良性 WARN
  （min_forward_gap / 变道场景的 lateral excursion）。
- 已登记进 `CLAUDE.md`（gate 清单）。

这条门禁补的正是 §3.1/§3.2 暴露的空洞：**2026-09-22 之前 sensor 模式的识别率与
预警提前量都是结构性 FAIL，而 CI 里没有任何一条在跑 sensor 编排**——能力烂掉也没人知道。

### 14.3 距"切默认"还差什么

sensor 模式已验证可信（14.1），剩下的是**把默认切过去**这一步，卡在一条协调上：

- 切 `config/pipeline.json`（+ windows/cortex 变体）到 sensor 编排后**必须重生成
  `tests/baseline/*.json`**——两种编排的行为本来就不同（如 urban_challenge
  x_delta 490 vs 522），旧 baseline 会把这次切换判成回归。
- 而**工作区里还有别人未提交的 planning 改动**（`planning_node.cpp` +
  `CubicSpline2D.cpp`），现在重生成 baseline 会把未提交的行为烤进去 → 之后必然对不上。
  所以顺序只能是：对方的 planning 改动先落地/提交 → 再切默认 + `--update-baseline`。

在此之前，14.2 那条 CI 门禁已经保证 sensor 编排不会退化。

---

## 15. 追加（同日续）：方向二起点——暴露权威车道级定位，并据此挖出一个车道模型不一致

### 15.1 侦察结论：能力已存在，缺的是"对外契约"

方向二（HD-Map / 车道级匹配）本来的缺口并不是"没有实现"，而是：

- **`map.json` 的 `roads[].lanes[].centerline[]` 确实被 `resolve_map_reference` 原样注入
  `road_network_json`，但运行时没有任何消费方用整条折线** —— A* 建图只取首末点
  （`scenario_router.c:514-520`，把每条车道退化成直线段），`json_to_xodr` 只用
  "车道数 + 宽度" 分条带。于是全栈"我在哪条车道"都退化成
  **"道路中心线 + `lane_count`/`lane_width` 的一维推算"**（只在道路沿 +x 时成立）。
- **唯一权威是 esmini 的 `FlowRoadNetwork::world_to_frenet`**（任意朝向，返回
  `{road_id, lane_id, s, offset}`），但它是**进程全局单例、非线程安全、flowsim 独占**，
  其它节点拿不到句柄 → 只能由 flowsim 发布。
- `vehicle/state` 里虽然有 `road_id`/`lane_id`，但那是**生成时刻的快照**
  （只在 `populate_entities_from_scenario` 写一次，`flowsim_node.cpp:740/741/756/757`），
  变道/掉头后不刷新 —— 这也解释了为什么 planning 只敢用它的**符号**。
- 反推论：**新写一个纯逻辑 lane matcher 会撞仓库铁律"同一功能第二份实现=违规"** ——
  权威已经存在，该做的是**暴露它**。

### 15.2 本 commit 做了什么（纯加法，零行为变更）

新增 topic `localization/lane_match`（登记在 `include/topic_registry.h`）：
flowsim 每帧用权威解算重新求 ego 的 `(road_id, lane_id, s, offset)` 并发布，**同时**
把它与 `vehicle/state` 里那份快照做对比、把漂移量化成 `id_mismatch_frames`。
monitor 透传到 `/tmp/flow_topology.json` 的 `metrics.lane_match`。
按仓库既有遥测惯例（同 `planning/debug`）：不进 `s_outputs`/`publish[]`、
不动 `topic_contract` 的声明侧门禁、不动 `msg_layout`。

| 文件 | 改动 |
|---|---|
| `include/topic_registry.h` | 新增 `TOPIC_LOCALIZATION_LANE_MATCH` + 契约说明 |
| `modules/adas_nodes/flowsim_node.cpp` | `publish_lane_match()`（权威解算 + 快照对比 + 计数）、主循环调用、advertise |
| `modules/adas_nodes/monitor_node.c` | 订阅 + 透传到 `metrics.lane_match` |
| `docs/HANDOFF_2026-09-22.md` | 本节 |

实测（`lane_change_traffic` 22s，29 帧样本）：topic 正常，`ok=true`、
`s` 随行驶单调推进（5.3 → 52.6 → 147.0，Δs 与里程一致）、`offset` 随变道增长；
`id_mismatch_frames` 在开场 7 帧后不再增长。

### 15.3 ⚠️ 顺带量化出的真问题：一日一维车道模型 × 地图车道布局不一致

新诊断一上线就把 `lane_change_traffic` 里那条 "lateral excursion 7.00m" WARN 的根因照出来了：

| ego_y | 权威 `lane_id` | 权威 `offset` | planning `ego_d` | `n_lanes` |
|---|---|---|---|---|
| -1.73 | -1 | +0.02 | -1.75 | 4 |
| +1.65 | -1 | +3.40 | +1.65 | 4 |
| +4.76 | -1 | **+6.51** | +4.61 | 4 |

- 实测恒等关系 `offset = ego_y + 1.75` → **权威模型里车道 -1 的中心在 y=-1.75**，
  而运行期 xodr（`/tmp/flowsim_lane_change_traffic.xodr`）里 4 条车道
  （`id=-1..-4`）**全在参考线右侧**（y = -1.75 / -5.25 / -8.75 / -12.25）。
- 而 planning 的 `lane_center_d(idx) = -(idx-(N-1)/2)·w` 把 N 条车道**对称铺在中心线两侧**
  （±1.75 / ±5.25）→ 只有右半两条与地图重合，**左半两条是地图上不存在的幻影车道**。
- **W1 那次"单向路放开左半幅"的修复**（`own_side_only = on_return || !oneway`）
  正好让 ego 合法地开进这些幻影车道 → 实测 ego 跑到 y=+4.76，而权威模型说它
  **离最近车道中心已 6.51m ≈ 1.9 个车道宽（3.5m）**，即**开到地图上没有路的一侧**。

**没有任何现有门禁抓它**（`lane_change_traffic` 在全量回归里是 PASS，那条 excursion 只是 WARN）。

### 15.4 后续（按价值排序）

| 优先级 | 项 | 说明 |
|---|---|---|
| **P1** | 加"权威车道越界"绊线 | 用 `lane_match.offset` 与 `road/geometry.lane_width` 比：`\|offset\| > w/2 + 余量` 即"在地图的车道外" → 计数 + 限频 WARN（先诊断，不阻断）。这是把 15.3 变成可长期盯的量 |
| **P1** | 车道布局模型统一 | 要么让 planning 消费权威车道布局（`lane_id`+`offset`+`s`），要么修正 `lane_center_d` 的"对称铺开"假设对单向路的适用性。**会改行为 → 需重生成 baseline**，同样受"工作区有他人未提交改动"阻塞（见 §14.3） |
| P2 | `json_to_xodr` 的单向路车道布局约定 | 现在单向路把 N 条车道全放右侧；要确认这是不是与场景/规划共同约定的语义（若是，则问题在 planning；若不是，则在生成器） |
| P2 | 让 flowsim 刷新 `vehicle/state` 的 `road_id`/`lane_id` | 把快照改成每帧权威值（`id_mismatch_frames` 可先用来判断该不该改；本场景只开场 7 帧漂移，暂不紧急） |
