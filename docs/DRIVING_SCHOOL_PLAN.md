# KunAutoDrive 驾校计划 — 科目一至科目四

> **状态：设计文档（实施完成后删除本文件）**
> 目标：让自动驾驶栈像人类学员一样通过驾校考试——每科有场景、有判定标准、有
> 扣分制评分、有 CI 门禁。"学会开车" = 全科 ≥90 分且持续不退步。
>
> 总原则（与项目铁律一致）：
> - **Python 仿真先行**：所有新场景先在 `tools/` 纯 Python 仿真里跑通+扫参，再动 C++
> - **模块职责分明**：仿真=被控对象（只积分物理），控制=唯一开车的人，规划=唯一决定去哪
> - **考试即门禁**：每科考试脚本可独立跑、可进 CI，不及格 = 阻断合并

---

## 全景课程表

| 科目 | 内容 | 检验对象 | 载体 |
|------|------|---------|------|
| 科目一 | 交规理论：红绿灯/限速/实线/让行优先级 | 规则仲裁层（架构收敛） | 交规仲裁模块 + 合规扣分 |
| 科目二 | 场地五项：倒库/侧方/坡道/S弯/直角 | **车辆模型+低速控制**（底子） | `tools/subject2_sim.py` |
| 科目三 | 道路驾驶：起步→跟车→变道→超车→掉头→靠边停车 | 行为/规划/控制全链路 | 考试卷场景 + evaluator |
| 科目四 | 安全文明：应急处置（爆胎=执行器降级、行人鬼探头、事故避险） | safety_control/degrade | 应急场景注入 |
| 考试制度 | `tools/driving_test.py` 统一入口，扣分制，<90 不及格 | 防回归 | CI 门禁 |

**实施顺序：科目二 → 考试框架 → 科目三 → 科目一 → 科目四**
（科目二直接检验车辆模型底子，是当前掉头 bug 的自然延伸；考试框架定标准；
科目三串联现有能力；科目一/四是架构收敛，风险最低放最后。）

---

## 第一期：科目二场地五项（本期核心）

### 为什么科目二最先做

科目二全是**低速 + 倒挡 + 满舵**工况——正是"掉头屁股横扫"bug 暴露的盲区：

| 现有盲区 | 科目二逼出它 |
|---------|-------------|
| steer 限幅钳在 [0.016, 0.16]，满舵 0.6 不可达 | 倒库/直角转弯必须满舵 |
| 倒挡路径跟踪从未测过（只有倒车速度限制） | 倒库/侧方全程倒挡 |
| 后轴零侧滑不变量只有单测，无场景级验证 | 压线判定直接暴露侧滑 |
| 低速 PID（<2m/s）从未标定 | 坡道定点要求 ±30cm 停准 |
| 无坡度物理（`step_bicycle` 无重力分量） | 坡道起步需要防溜车 |

### 新文件（唯一新增入口，不改 control_sim.py）

```
tools/subject2_sim.py        # 场地五项仿真+判定+扫参（纯 Python 标准库）
tools/driving_test.py        # 考试框架（第一期只挂科目二，后续挂三/一/四）
```

**复用 control_sim.py 的方式**：`from control_sim import VehicleState, stanley_control, ...`
（control_sim.py 是模块可 import；禁止复制粘贴第二份自行车模型——单一权威）

**需要对 control_sim.py 做的最小扩展**（三处，都是加参数不改行为）：
1. `VehicleState.step()` 加 `grade=0.0` 参数：`self.v += -9.81*sin(grade)*dt`（坡道重力分量，默认 0 不影响现有场景）
2. `VehicleState.step()` 的速度上限 `CRUISE_SPEED+2` 改为可选参数（科目二低速不受影响，保持默认）
3. 暴露 `STEER_FULL_LOCK = 0.60`常量（对应 entity.h steer_override=true 的物理限幅）

### 五项场景规格（判定标准 = 真实驾考标准的仿真化）

坐标约定：场地局部坐标，单位 m，车辆尺寸 4.6×2.0（与 straight_road.json 一致），轴距 2.7。

#### 1. 倒车入库 (reverse_parking)
- **场地**：库位宽 2.4（车宽+0.4，考试标准 1.5 倍车宽这里从严）、深 5.5；两侧库线；控制线距库 7.5
- **流程**：前进过控制线 → 倒挡右倒入库 → 停正 → 前出 → 左倒入库 → 停正
- **判定**：
  - 车轮压库线 → **不合格**（车身四角多边形与库线线段相交检测）
  - 中途停车 >2s → 扣 5 分/次
  - 车身入库后与库中线夹角 >5° → 扣 10（停不正）
  - 总时长 >210s → 不合格
- **暴露什么**：倒挡满舵路径跟踪、后轴运动学正确性（屁股先入库=正确物理！）

#### 2. 侧方停车 (parallel_parking)
- **场地**：库长 6.0（车长×1.3）、宽 2.4，右侧贴路沿
- **流程**：平行驶过库位 → 倒挡右打→左打 S 形入库 → 停正 → 打左灯前出
- **判定**：压库线/路沿不合格；中停 >2s 扣 5；出库不打灯扣 10；>90s 不合格
- **暴露什么**：倒挡 S 形（转向反打时机）、与倒库互补的左右对称性

#### 3. 坡道定点停车与起步 (hill_start)
- **场地**：坡度 10%（grade=atan(0.1)），坡长 20，定点桩在坡中
- **流程**：上坡 → 前保险杠对桩停车 → 停稳 ≥2s → 起步通过坡顶
- **判定**：
  - 停车点纵向偏差 >0.5m 不合格，>0.3m 扣 10
  - 起步后溜 >0.3m 不合格，>0.05m 扣 5（**关键指标：防溜坡**）
  - 起步超时 >30s 不合格
- **暴露什么**：低速 PID 精度、坡度重力补偿（现有 PID 无前馈必溜坡——预期先 FAIL，逼出坡道前馈）

#### 4. 曲线行驶/S弯 (s_curve)
- **场地**：两段相接的反向圆弧车道，半径 7.5、车道宽 3.5
- **判定**：任一车轮出车道边线不合格；中停 >2s 扣 5
- **暴露什么**：连续大曲率跟踪（κ=0.13，远超高速路的 0.005）、转向反打平顺性

#### 5. 直角转弯 (right_angle_turn)
- **场地**：两条宽 3.5 的垂直车道相接
- **流程**：直行 → 贴外侧 → 满舵直角转过 → 摆正
- **判定**：压角/压线不合格；中停扣 5；不打转向灯扣 10
- **暴露什么**：满舵时机决策（早打压内角、晚打出外线）——纯几何规划能力

### 通用判定基础设施（subject2_sim.py 内部）

```
car_polygon(state) -> 4 角点        # 车身多边形（含 heading 旋转）
poly_intersects_segment(poly, seg)  # 压线检测（每帧调用）
StopWatch                            # 中途停车计时（v<0.05 连续时长）
Scorecard                            # 扣分制记分卡：100 起步，逐项扣，输出明细
```

### 控制/规划层怎么开科目二的车（学员实现）

科目二不用 Stanley 巡航——用**几何路径 + 纯追踪(Pure Pursuit) + 倒挡镜像**：

1. 每项场景先离线生成参考路径（圆弧+直线拼接，倒库路径=两段圆弧对接）
2. 前进：Pure Pursuit（前视距离 = max(1.5, 0.5·v)）；倒挡：参考点换车尾、转向取反
3. 纵向：目标速度阶梯（直线 1.5 m/s，弯内 0.8，定点用距离-速度斜坡）
4. **参数扫描**（`--tune-s2`）：前视距离/满舵时机/入库起手位，各项独立扫

这层"学员"逻辑先活在 subject2_sim.py 里验证方法论；第二阶段才决定
哪些进 C++（预计：Pure Pursuit 倒挡跟踪 → control_node 的 maneuver 分支；
路径生成 → planning 的泊车 planner）。**本期不动 C++**。

### CLI 设计

```bash
python3 tools/subject2_sim.py --item reverse_parking   # 单项
python3 tools/subject2_sim.py --all                    # 五项连考
python3 tools/subject2_sim.py --tune-s2 reverse_parking # 扫参
python3 tools/subject2_sim.py --all --csv out/         # 轨迹 CSV（可画图复盘）
```

### 验收标准（本期 DoD）

- [ ] 五项场景 + 判定几何全部实现，`--all` 出五张记分卡
- [ ] 五项全部 ≥90 分（先跑通再扫参；坡道预期先不及格，加重力前馈后过）
- [ ] 判定器自测：故意喂"压线轨迹/溜坡轨迹"必须抓到（门禁有效性自检，项目传统）
- [ ] 物理一致性：subject2 用的 VehicleState 与 physics.cpp 单测数值对齐（满舵直径 8.34m 已有基准）

---

## 第二期：考试框架 driving_test.py

```bash
python3 tools/driving_test.py --subject 2          # 单科
python3 tools/driving_test.py --all                # 全科（已实现的科目）
python3 tools/driving_test.py --all --ci           # CI 模式：不及格退出码非 0
```

- 统一记分卡输出（JSON + 终端表格），保存 `out/driving_test_report.json`
- baseline 对比：`--update-baseline` / 默认与 baseline diff，**分数下降 >5 即 FAIL**（防退步）
- 挂进 CI（与 `scenario_regression.py` 并列一档）

## 第三期：科目三道路考试卷

- 一条 3km 考试路线场景 `scenarios/subject3_exam.json`：串联
  起步(打灯+观察) → 跟车 → 变道 → 超车 → 会车 → 红绿灯路口 → 掉头 → 靠边停车(30cm)
- 判定复用 demo_evaluator 采样管道 + 新增逐项评分器（每个动作段独立打分）
- **依赖**：红绿灯 choreography 锁死问题需先修（当前 t=10 red beat 疑似锁灯）；
  掉头依赖 off-rails 修复合入
- 靠边停车 30cm 判定直接复用科目二的压线/距离几何库

## 第四期：科目一交规仲裁层（架构收敛）

- 现状：红绿灯逻辑在 planning、TTC 在 safety、让行在 planning Phase5——**同一职责三处实现**
- 收敛为 `src/algorithms/traffic_rules.c`：输入(ego, 障碍, 灯, 路)→输出(速度上限, 禁行区, 让行标志)，
  planning 只消费仲裁结果
- 合规扣分项进考试：闯红灯/不让行 = 直接不合格
- **注意**：这是重构，commit 必须带 `Removed:` 段，旧散逻辑同 commit 删

## 第五期：科目四应急

- 场景注入：行人鬼探头（已有雏形）、前车急刹、执行器降级（steer 卡死→degrade 接管）、爆胎（附加横向偏置力）
- 检验 safety_control + degrade_ladder 的真实处置（正好覆盖本次修的降级自恢复）

---

## 风险与依赖

| 风险 | 缓解 |
|------|------|
| 坡道物理 C++ 侧没有（physics.cpp 无 grade） | 第一期只在 Python 加；C++ 移植时同步给 step_bicycle 加 grade 参数+单测 |
| 倒挡控制 C++ 侧只有速度限幅没有跟踪 | 正是要逼出来的能力缺口，Python 先证明方法 |
| control_sim.py 已 2300 行 | 科目二独立成 subject2_sim.py，只 import 不往里塞 |
| 红绿灯锁死阻塞科目三 | 科目三排第三期，等该修复落地 |
| 与他人 WIP（planning/behavior）冲突 | 第一/二期纯 tools/ 新文件，零冲突 |

## 里程碑

1. **M1（第一期）**：subject2_sim.py 五项 ≥90 + 判定器自测过
2. **M2（第二期）**：driving_test.py 挂 CI，baseline 防退步生效
3. **M3**：科目二学员逻辑移植 C++（泊车 planner + 倒挡跟踪），demo 里演示倒库
4. **M4（第三期）**：科目三考试卷全程 ≥90
5. **M5（四/五期）**：交规仲裁层收敛 + 应急场景，全科毕业 🎓

> 实施完成后：删除本文档，把"驾校考试"作为常设门禁写入 CLAUDE.md 验证章节。
