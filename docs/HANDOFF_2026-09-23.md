# 交接：2026-09-23 —— L3 §17.2/§17.4 根因翻案 + 修复（空障碍物列表 ≠ 没就绪）

> 上一位接手人（`HANDOFF_2026-09-22.md` §17.2）判断：sensor 模式下 `straight_road`
> 冲出路面是 §13 的**簇分裂**引发"幽灵避让"。**本批用 A/B 证据推翻这个判断**，
> 定位到两个真实缺陷并修掉，同时补了一条能抓住这次故障的门禁。

---

## 1. 症状（复现）

`sensor` 编排（`FLOW_PIPELINE=config/pipeline_sensor.json`）跑 `straight_road`
（4 车道双向，半宽 7m，ego 起点 x=20 朝 -x）：

| | `behavior/state` 采样 | ego x 范围 | max\|y\| | 评估器 |
|---|---|---|---|---|
| 修复前 · sensor | **NA 301/475**（前 30s 全静默） | −9.6 … 89.3 | **12.73 m**（出路面） | FAIL：road departure 6.80m / 74 帧 |
| 修复前 · ground_truth | NA 4/478（仅启动瞬间） | 16.2 … 535.7 | 9.14 m（掉头窗口） | PASS |
| **修复后 · sensor** | NA 5/475，`U_TURN` 144 | 16.2 … 276.4 | **5.12 m**（路内） | **PASS**（剩 U 型掉头横向 WARN） |

对照点：ground_truth 在 t≈3–8s 就完成掉头并朝 +x 走；sensor 从不掉头，
一路把车开到路线尽头 x≈0 之外，在无路区域横向漂到 y=12.7 ——
**§17.2 看到的"planning 想稳车道却被横向挤出去"，因果方向反了：不是被挤出去，
是"没人下掉头指令"，车自己开过了路网尽头。**

## 2. 根因一：空障碍物列表被当成"感知没就绪"（行为决策整块被跳过）

`modules/adas_nodes/behavior_planner_node.cpp` `on_raw_obstacles()` 原先：

```c
if (obs_list.count == 0) return;      /* ← 空列表直接返回 */
```

后果链：`has_obs` 恒 0 → `run()` 前置守卫（`if (!g.has_fusion || !g.has_obs) return;`）
把**整个决策块**跳过 —— 包括路端掉头触发（`UTURN_TRIGGER`）。而车还在动，
所以既有的量活性门禁（speed/x/heading/steer/lane_count 全部有值）**全绿**。

为什么只在 sensor 模式暴露：ego 起步背对路线（视锥 120° 内空无一物），
真点云链路"正确地"连着发**空** `perception/obstacles`；ground_truth 直通编排
一开始就有 19 个真值目标，所以 `has_obs=1` 从未缺席。同一 bug 在 `curve_road`
（该场景本就没有可观测 actor）**默认编排里也一直在发作**：见 §4。

**修法**：空列表是**合法感知结果**（视野内无目标），不是"还没数据"。
置 `obs_count=0` + `has_obs=1`，让决策在"无目标"这个真实状态下继续工作。

## 3. 根因二：清零的未用槽位在世界原点变成"幽灵障碍物"

`modules/adas_nodes/planning_node.cpp` `on_perception_obstacles()` 每帧把 128 个
槽位全部清零再填 `list.count` 个；但 TTC 跟车 / 窄路会车 / Frenet 避障注入 /
ST 图障碍四处循环**扫满 128 槽**，于是 `(0,0)` 被当成"世界原点有个静止障碍物"。
自相矛盾的是同一份数据在 `forward_space` 里有一条 `if (obs_x==0 && obs_y==0) continue;`
豁免 —— 一个文件里两套有效性语义。

影响面：ego 所在车道经过世界原点前后 80m 内就会**幽灵刹车**（真实部署里原点
可以是任意地图坐标）。**实测证据**（`straight_road`，两种编排都命中）：

```
# 修复前（默认编排 logs_gt、sensor 编排 logs_sensor 都在同一时刻出现）
TTC follow: gap=20.0 (src=percept) -> 3.7 m/s (was 20.0)
TTC follow: gap=19.7 (src=percept) -> 3.7 m/s (was 20.0)   ← gap 随 ego 逼近原点递减
TTC follow: gap=12.5 (src=percept) -> 1.9 m/s (was 20.0)
# 修复后（默认编排回归日志）
TTC follow: gap=77.1 (src=percept) -> 18.0 m/s (was 20.0)  ← 只剩 80m 外的真障碍物
```

ego 起步 x=20 朝 -x，世界原点恰在前方 20m 同车道 → (20−5)/4 = 3.7 m/s。
注意这条链在 `straight_road` 的**committed 基线**里同样生效（基线
`avg_speed_mps = 12.0` 是在被压到 3.7 m/s 起步的条件下测出来的）。

**修法**：引入权威计数 `g.obs_count`（与既有 `truth_obs_count` 对称），
四处循环改为 `i < g.obs_count`，并删除那条 `(0,0)` 补丁式豁免。

## 4. A/B：两个 FAIL 场景到底是谁造成的

方法：`git stash` 两处 C++ 改动 → 重建节点 → 跑场景 → 恢复 → 重建 → 再跑
（脚本 `~/.hermes/cache/scratch/ab_two_scenarios.sh`）。改动前/后各一次：

| 场景 | 改动前 | 改动后 |
|---|---|---|
| `curve_road`（默认编排） | FAIL —— **新门禁抓到 `behavior/state == "NA"` 全部 251 样本** + `exceeded max duration 62.7s` | FAIL —— **只剩** `exceeded max duration 62.8s`（行为已 CRUISE） |
| `lane_change_traffic` | PASS | 第 1 次 FAIL `min_forward_gap 4.80m < 9.27m`；第 2 次 PASS（`min_forward_gap 10.95m` WARN） |

两条结论，都写清楚以免下次误判：

1. **`curve_road` 的时长 FAIL 与本次改动无关**：改动前 62.7s、改动后 62.8s，
   同值；而 `curve_road` 提交在库的基线是 59.9s。这台机器（WSL，负载）比 CI
   慢约 2s 启动/收尾 → 这是**环境敏感的墙钟度量**问题（见 §6 待办 1）。
2. **`curve_road` 的 committed 基线是在 behavior 静默状态下采的**
   （`tests/baseline/curve_road.json` → `behavior_state: "NA"`, `behavior_obs_count: 0`）。
   也就是说该场景的"绿灯基线"由一台**行为决策已死**的车跑出来，且此前无门禁能看见。
   修复后它变成 `CRUISE`，行为判据照样 PASS，但基线语义已经过期。
3. `lane_change_traffic` 的跟车距离**本来就在判据阈值附近**（50% × desired 18.5m =
   9.27m）。去掉幽灵障碍物后更多时间落在 4.8–11m 区间 → 判定在 FAIL/PASS 间跳。
   当时的读法是"幽灵障碍物掩盖了 ACC 跟车过近"，**后续证伪**：见 §10 ——
   那几帧的 ego 速度只有 1.0 m/s（期望间距 6.5m），是判据用整段中位速度当参考
   造成的**度量误报**，不是跟车控制缺陷。

## 5. 本批新增的门禁（有牙，已自证）

`ci/evaluators/demo_evaluator.py` 新增判据：

- `behavior_planner` 在跑 且 `behavior/state` **全程** `"NA"`/空 → **FAIL**
  （`behavior planner never decided: ...`）；
- 半数以上样本为 `"NA"` → **WARN**（启动慢/上游稀疏，不阻断）；
- series 不带 `metrics.behavior`（单测构造 / 无 behavior 的 profile）→ 静默跳过。

配套测试：

- `ci/evaluators/test_evaluator_gate.py` 新增 [20] 用例（门禁自检 **31/31**，原 29）；
- `ci/evaluators/test_demo_evaluator.py` 新增 3 用例（FAIL / WARN / 不适用；
  单测 **33/33**，原 30）。

**有效性证明**：这条门禁是在**未修 C++** 的那次 A/B 里第一次抓到 `curve_road`
（`"NA" in all 251 samples`）——即它抓的是真实链路故障，不是构造样本。

## 6. 剩余清单（切 sensor 默认为止，按顺序）

`HANDOFF_2026-09-22.md` §17.4 的前置条件据此改写。**原来 5 条前置现在只剩 2 条**：

1. ~~§13.5 (b) 碎片合并~~ → **不再是前置**。碎片确实存在（§13 探针已量化），
   但 `straight_road` 冲出路面的根因是 §2/§3 两条，与碎片无关，碎片合并另行排期。
2. ~~`max_duration_s` 度量的环境敏感~~ → **已修**（见 §12）：判据改用**仿真钟**
   （`metrics.scene.t_us`）而非墙钟跨度；墙钟超而仿真没超 → WARN。
   `curve_road` 两种编排现在都 **PASS**。
3. ~~ACC 跟车距离~~ → **已定性为度量误报并修掉**（见 §10）：判据用整段**中位速度**
   当期望间距参考，把"低速逼近/排队停下"的帧按高速帧的尺度判 FAIL。改为**逐帧**
   用该帧车速（与 behavior 跟车策略同式同参）。planning 的跟车控制**没有**缺陷
   （最差帧 ego 仅 1.0 m/s、gap 4.96m，该速度下期望 6.5m）。
4. ✅ **重采 8 场景基线** → **数据结论：不需要重采**（2026-09-24，见 §16.2）：
   三场景实测基线 ≈ 3 趟中位（均 1.00×），且同一份代码的抖动未撞穿任何门禁；
   单样本重采对 `lane_change_traffic`（极差 29%）是**净风险**（可能把门槛放松）。
   原两条"历史包袱"理由：`straight_road` 的 12.0 已被审计否掉；
   `curve_road` 的陈旧项只在不被数值门禁管的 `behavior_state` 字符串上，重采数值无收益。
5. ~~⛔ §17.3（sensor 模式下 `city_comprehensive`/`multi_light` 声明车辆不可观测）~~
   → **已消解，无需改 fixture**（2026-09-24 实测，sensor 编排）：
   `city_comprehensive` **PASS**、`multi_light` **PASS**，`truth_count_vehicle`
   分别 **192 / 498**（原为 0，故当时判 INCONCLUSIVE）、`recognition_rate_vehicle=1.000`、
   `perception_coverage=1.000`。残留已知项（不影响该场景判定、另账）：
   两者 `warning_lead_avg_s=0.000` —— **已追因，见 §16.3：这两个场景整段零临界事件**
   （fixture 只声明 1 / 2 个 actor、全程没进 3s TTC）→ 空集按 0.0 报。既不是感知失效，
   也不是"簇 id 每帧变、跨帧跟踪失效"（评估器算提前量**不用感知/航迹 id**，按真值身份
   + 位置匹配）。另：`perceived_count_avg` 仅 1~2，这两个场景在 sensor 下目标本就稀疏。
6. 以上都清了，才轮到：切 `pipeline.json`/`pipeline_windows.json` +
   `--update-baseline` + 自证。

## 7. 复现命令

```bash
# 构建（改的是节点插件，必须重建子工程）
cmake -B build/modules/adas_nodes -S modules/adas_nodes -DFLOWENGINE_BUILD=build
cmake --build build/modules/adas_nodes -j$(nproc)

# 单测 + 门禁自检
env -u LD_LIBRARY_PATH ./build/bin/test_adas_nodes_logic            # 54/54
python3 ci/evaluators/test_demo_evaluator.py                        # 35/35
python3 ci/evaluators/test_evaluator_gate.py                        # 33/33

# 症状场景：sensor 编排 straight_road（修复前 FAIL road departure → 修复后 PASS）
FLOW_PIPELINE=config/pipeline_sensor.json \
  python3 ci/evaluators/scenario_regression.py --workers 1 --only straight_road

# CI 的 sensor 专用步骤（三个场景，不带 --baseline）
for s in urban_challenge dense_npc lane_change_traffic; do
  FLOW_PIPELINE=config/pipeline_sensor.json \
    python3 ci/evaluators/scenario_regression.py --workers 1 --only "$s"
done

# 跟车 gap 判据：把任意 result JSON 的 min gap 帧还原成 "gap @ ego 速度"
python3 ~/.hermes/cache/scratch/gap_frame_analysis.py /tmp/flow_regression/lane_change_traffic.json

# 默认编排全矩阵（本机最终：**8/8 PASS**，exit 0，对 committed 基线零 regression）
python3 ci/evaluators/scenario_regression.py --baseline --workers 1

# A/B（改动前 vs 改动后），产物在 /tmp/ab/
bash ~/.hermes/cache/scratch/ab_two_scenarios.sh

# 诊断采样（behavior 状态 / ego 轨迹时序）
FLOW_PIPELINE=config/pipeline_sensor.json \
  python3 ~/.hermes/cache/scratch/diag_sample.py scenarios/straight_road.json 45 out.jsonl
```

## 8. 不要做的事

- ❌ 不要再把 sensor 模式 `straight_road` 的出走归因于**簇分裂**（§17.2 的假设已被
  A/B 推翻）；先看 `behavior/state` 是不是 `NA`。
- ❌ 不要为了过 `max_duration_s` 直接把 `+1.0s` 放大 —— 那会把"真挂死"一起放掉。
- ❌ 不要把 `on_raw_obstacles` 的 `count == 0` 再改回 `return`（会重新把行为决策
  整块静默跳过；门禁会红，但根因分析要重来一遍）。
- ❌ 不要在 planning 的障碍物循环里回退到 `i < kMaxObs` 或重新引入 `(0,0)` 豁免
  —— 有效性由 `g.obs_count` 一家说了算。

## 9. 待办：`CLAUDE.md` 故障模式表两条新行（需你批准）

`CLAUDE.md` 是受保护的 agent 指令文件，本批对该文件的写入**未获批准**（审批超时），
故这两行先落在本文件，请你在方便时并入 `CLAUDE.md` 的「常见故障模式」表：

1. **行为决策整块静默 30s（该掉头不掉、该变道不变），而车仍在动 → 所有量活性门禁全绿**
   → 根因：`behavior_planner on_raw_obstacles` 把空障碍物列表当"感知没就绪"直接
   `return` → `has_obs` 恒 0 → `run()` 前置守卫跳过整个决策块（含 `UTURN_TRIGGER`）。
   → 位置：`behavior_planner_node.cpp` `on_raw_obstacles` /
   `demo_evaluator.py` 行为活性判据。
2. **幽灵刹车（本车道前方 80m 内有"静止障碍物"，实际空无一物）**
   → 根因：planning `on_perception_obstacles` 清零未用槽位后，四处循环扫满 128 槽 →
   `(0,0)` 被当成世界原点的障碍物（同文件另有一处 `(0,0) continue` 豁免，两套语义）。
   → 位置：`planning_node.cpp` `on_perception_obstacles` + TTC/会车/Frenet/ST 四处循环。
3. **场景判定在 PASS/FAIL 之间摇摆（同一份代码，重跑一次结论就变）**
   → 根因：两条判据的"参考量"与它要守的策略不是同一套尺度 ——
   (a) 跟车 `min_forward_gap` 用**整段中位速度**算期望间距，低速逼近/排队停下的帧
   被高速帧的尺度判 FAIL（实测最差帧 `gap=4.96m @ ego 1.0 m/s`，该速度下期望 6.5m）；
   (b) `max_duration_s` 用**墙钟跨度**（含 demo.sh 启动/收尾与机器负载，同场景
   59.9→63.3s 漂移），而仿真钟只有 59.3s。
   → 修复：(a) 逐帧用该帧车速（与 behavior 的 `acc_standoff + acc_time_headway·v`
   同式同参）；(b) 改按 `metrics.scene.t_us` 的仿真空跨度，墙钟超而仿真没超记 WARN。
   → 位置：`demo_evaluator.py` `min_forward_gap` 判据 / `_sim_timestamps` + `max_duration_s` 判据。

## 10. 追加：跟车 gap 判据的度量误报（同类于 §17.1，已修）

`lane_change_traffic` 在默认编排下 FAIL/PASS 摇摆（同一份代码：`min_forward_gap
4.80m / 4.96m` FAIL，`10.95m / 43.2m` PASS）。逐帧测量后定性为**度量误报**：

```
# FAIL 的那趟（v_med=8.98 → desired(v_med)=18.47 → 阈值 9.24m）
最差移动帧  gap=4.96m  ego_v=1.00 m/s  beh=CRUISE   ← 该速度下期望 6.50m，阈值 3.25m
最差任意帧  gap=2.98m  ego_v=0.00 m/s  beh=FOLLOW   ← 排队停在静止车前
157 个有限 gap 帧：按 v_med 判据 FAIL 而按本帧速度安全 = 28 帧；按本帧速度 FAIL = 0 帧
# PASS 的那趟：v_med=16.99（全程没被压到低速），min gap 43.2m —— 帧统计 0/0
```

> **证据存档**：跑完的原始 result JSON 很大（每份含全量 samples，另加 worker 目录，
> 本批累计 ~3.3GB），已清掉并保留精简版在
> `~/.hermes/cache/scratch/evidence_20260923/`（`results_slim.json` = 关键结果 +
> summary 字段；`matrix_*.txt` = 三次矩阵表）。重跑命令见 §7。

**判据用整段中位速度当唯一参考**，于是"低速逼近 / 排队停下"的帧被高速帧的期望
间距误判；而 `demo_evaluator` 自己那条注释写的是"判据随车速伸缩"——实现与自述
矛盾（behavior 的跟车策略 `desired_gap = acc_standoff + acc_time_headway * ego_v`
本来就是逐帧的）。

**修法**：逐帧与**该帧车速**的期望间距比（`ACC_STANDOFF_M + ACC_TIME_HEADWAY_S·|v|`，
与 behavior 同式同参；50% 阈值不变）。高速贴近毫不放松：20 m/s 期望 35m、阈值 17.5m。
WARN 分支保留（对"整段中位速度"参考，措辞已标明，不再暗示违规）。

- 单测：`test_demo_evaluator.py` +4 子例（低速逼近不 FAIL / 20 m/s 贴 10m 仍 FAIL /
  静止排队不 FAIL / gap≤0 仍 FAIL）→ **34/34**；
- 门禁自检：`test_evaluator_gate.py` +[21]（高速贴近仍抓得住 + 低速不误报）→ **33/33**。
- 工具：`~/.hermes/cache/scratch/gap_frame_analysis.py`（把任意 result JSON 的
  min_forward_gap 帧还原成"gap @ ego 速度"并同时给出两套判据的结论）。
- **planning 的跟车控制没有缺陷**：最差帧 ego 只有 1.0 m/s、gap 4.96m，该速度下
  期望 6.5m。当时"幽灵障碍物掩盖了 ACC 跟车过近"的读法已撤回（§4.3 已更正）。

## 11. 追加：sensor 链验证（本批改动后）

- CI 里那条 sensor 专用步骤的三个场景，本机 sensor 编排各跑一次：
  `urban_challenge` **PASS**、`dense_npc` **PASS**、`lane_change_traffic` **PASS**
  （结果摘要见 `~/.hermes/cache/scratch/evidence_20260923/results_slim.json`，
  复现命令见 §7；原始 result JSON 含全量 samples，跑完已清理）。
- `straight_road`（本批主战场）：sensor 编排 **PASS**、默认编排 **PASS**。
- `curve_road`：行为判据已恢复；时长的假 FAIL 也已修（见 §12），两种编排现在都 PASS。

## 12. 追加：`max_duration_s` 按仿真钟判（同类度量伪影，已修）

`curve_road` 在**两种编排**下都只剩一条 FAIL：`exceeded max duration: 62.7s / 62.8s /
63.3s > 60.0s`。A/B（§4）已证明它与算法改动无关（改动前 62.7s、改动后 62.8s 同值）。
逐样本核对后发现判据量的根本不是"仿真跑了多久"：

```
样本里的两个时间戳（curve_road，同一趟 run）：
  墙钟跨度 timestamp[-1]-timestamp[0] = 63.32s   ← 判据用的就是这个
  仿真钟   scene.t_us 跨度             = 59.30s   ← 场景声明 60s，仿真完全正常
```

`demo_evaluator` 的判据取自 monitor 的 `clock_now_realtime_us()`（墙钟，含 demo.sh
启动/收尾 + 机器负载），所以同一场景在本机会 59.9→63.3s 漂移，撞穿 `+1.0s` 容差。
**修法**：新增 `_sim_timestamps()` 从 `metrics.scene.t_us` 取仿真钟，`max_duration_s`
与"run truncated"两个跨度判据都改用它（取不到时回退墙钟，语义不变）；
`summary` 增 `sim_duration_s`（`duration_s` 保留墙钟语义，基线兼容）；
墙钟超而仿真没超 → **WARN**（保留可见性，不再当成"没跑完"）。

- 单测：`test_demo_evaluator.py` +2 组（墙钟超/仿真正常→不 FAIL 但有 WARN；
  仿真真超→FAIL；无仿真钟→回退墙钟语义不变）→ **35/35**；
- 门禁自检 [19]（truncated run）走墙钟回退路径，仍 **33/33**；
- 实测：`curve_road` 默认编排 **PASS**、sensor 编排 **PASS**。

## 13. 本批最终验证矩阵（本机，全绿）

| 检查 | 结果 |
|------|------|
| `test_adas_nodes_logic`（C，含 perception/dbscan/planning 逻辑） | **54/54** |
| `ctest -LE "benchmark\|manual\|stability\|integration"`（CI 等价快速套件） | **30/30** |
| `test_demo_evaluator.py` | **35/35**（本批 +5） |
| `test_evaluator_gate.py` 门禁自检 | **33/33**（本批 +4；§14 校准后再 +2 → 35/35） |
| CI 静态 gate ×5（topic_contract / msg_layout / plugin_symbol / zombie_ban / sensor_wiring） | 全 OK |
| `tools/scenarioctl.py validate` | OK |
| 默认编排 8 场景矩阵 `--baseline` | **8/8 PASS**，exit 0，零 regression |
| sensor 编排 `straight_road` | **PASS**（修复前 FAIL：road departure 6.80m / 74 帧） |
| sensor 编排 CI 三场景（urban_challenge / dense_npc / lane_change_traffic） | **PASS** ×3 |
| sensor 编排 `curve_road` | **PASS**（修复前 FAIL） |

改动已提交为 **`29ef6ea`** `fix(sensor-chain): 空障碍物列表静默行为 + 幽灵障碍物 + 两条门禁度量伪影`（9 文件，+726/−24），含 `CLAUDE.md` 故障模式表两行（§9 文本已并入）与 `.gitignore` 忽略 clangd 的 `.cache/`。

提交后合并树复验（另一 agent 的 `8ff9bfd` perception 碎片合并落在本批验证之后，故在合并树上重跑）：

| 检查 | 结果 |
|------|------|
| `test_adas_nodes_logic`（合并树） | **57/57** |
| sensor `straight_road` / `curve_road` / `lane_change_traffic` | **PASS ×3** |
| 默认 `curve_road` | **PASS** |
| 默认 `straight_road` | 见 §14（门禁伪影，已校准） |

## 14. 追加：steer 速率判据的"单点擦线"伪影（同类于 §17.1，已校准）

**症状**：合并树（含 `8ff9bfd`）上默认编排 `straight_road` 首跑 **FAIL**：
`steer bang-bang oscillation: steer_rate_rms=0.58/s, max=6.25/s, flips=0.06/s`，
同代码随后两趟 **PASS**（max 3.88 / 3.76）→ 门禁在窄带里抖。

**根因（不是控制抖动）**：

1. 触发项只有 `max_steer_rate`。同一判据的另两个量全程健康：`rms 0.39~0.58`（FAIL 线 1.6）、
   `flips 0.06`（FAIL 线 2.5；真 bang-bang 复发时实测 9~10/s）。
2. 逐样本定位：最高的 5 对样本 `behavior/state` 全是 `U_TURN`，最大一对
   `Δsteer=0.9005 rad / dt=0.239s = 3.76/s`（`-0.3005 → +0.6000`）；`|steer|=0.6` 的 20 个样本
   全在起手掉头段。
3. ±0.60 是掉头的**合法量程**：`flowsim/physics.cpp:57-60` —— 正常行驶限幅 0.25 rad（≈14°），
   掉头 `steer_override` 放宽到 0.60 rad（≈34°）。三把方向 lock-to-lock 扫角在一个采样间隔内
   Δsteer≈0.9 rad 是机动自身动力学，不是控制每帧翻符号。
4. 该门禁**没有机动窗口概念**且取整段 max（旁边 excursion 判据会写 "expected for cross-road
   maneuver"）；它的 FAIL 文案也印错了阈值（打印 flip 线 2.5，实际触发的是速率线 6.0）。

**校准（用户拍板选"加佐证条件"）**：

- `max_steer_rate > STEER_MAX_RATE_FAIL` 现在需 `steer_rate_rms > STEER_RATE_RMS_WARN(0.9)`
  佐证才 FAIL，单点擦线只留 WARN；`rms`/`flips` 两条 FAIL 线原样不动（先例：yaw 判据要求
  `yaw_rms` 佐证）。
- FAIL 文案改为印**真正生效**的三条阈值。
- 不加机动窗口豁免、不抬 `STEER_MAX_RATE_FAIL`：前者会让掉头自身的控制质量失去覆盖，
  后者会把真的单帧跳变一起放掉。
- 新增门禁自检 `[22]`（掉头扫角不得误报）+ `[23]`（max>6/s 且 rms>0.9/s 仍必须 FAIL，
  防"改成永远 PASS"）。门禁自检 **35/35**。

**证据快照**：`~/.hermes/cache/scratch/steer_spike_probe.py`（连跑 N 趟保留 dump，
逐趟定位 max|Δsteer|/dt 的样本对、dt、行为状态）。

## 15. 追加：sensor 全矩阵曾出现 2/8 失败 —— 已定性为**同场景并发跑**，非 harness 随机红

**现象（第一次全矩阵）**：sensor 编排 8 场景 → **6 PASS / 2 FAIL**（`urban_challenge`、
`multi_light`），两条都是 `no topology samples collected`；demo.sh 步骤 [3/4] 报
`✗ Timeout waiting for .../topology.json`。第二次全矩阵（单独跑）→ **8/8 PASS**，
零 `[diag]`、零采集超时。

**根因（已用实验证伪/证实，不是随机故障）**：当时我一边等矩阵一边起了"重跑这两个场景"的
第二个实例，两者**场景 key 相同 → 共用同一个 worker 目录与 `topology.json` 路径**
（`scenario_regression.prepare_worker_workspace` 按 `results_dir/.workers/<key>/` 隔离，
同一 key 就是同一份），于是：评估器启动时 `collect_samples` 会 `unlink` 该文件、
两个 monitor 交替覆写同一路径 → 一方采不到样本或采到两场仿真的交错样本。

**故意复现（同一场景并发两实例，各带诊断）**：双方都 FAIL，失败签名是

```
npc teleport: id=11 displaced 2843.6 m in one frame (>200m threshold, likely id-collision pollution)
ego yaw limit cycle: yaw_rms=2.90 rad/s, max=20.60   ← 健康值 0.02~0.16 rad/s
steer bang-bang oscillation: rms=1.47/s, max=13.18/s
```

**操作规则（写进故障模式认知）**：
- **绝不要对同一场景并发跑两个实例**（含"等一个矩阵时顺手重跑某场景"）。不同场景可并发
  （CI 的 `--workers N` 就是这么用的），相同场景必须串行。
- 采样污染的特征签名：`npc teleport` 达**千米级** + `yaw_rms` 达 **rad/s 级**（正常 0.0x）
  + `max_npc_speed` 上百 m/s —— 看到这组合先怀疑并发/共享路径，别去追控制算法。
- 相比之下 `no topology samples collected` 是同一根因的另一种表现（文件被对方 unlink）。

**已做（保留）**：`collect_samples` 的采集层诊断（`8ac1f48`）——0 样本时打印
`polls/missing/stale/invalid/valid` 与文件终态，用于区分"monitor 没写 / 被判陈旧 / 内容空"：

```
[diag] collected 0 samples — elapsed_s=12.5 final_exists=False invalid=0 missing=25 polls=25 stale=0 valid=0
[diag] json_file=/tmp/bogus_topo.json demo.sh_returncode=0
```

**对"切默认"的影响**：无。CI 各步骤串行、worker 按场景分片，不存在同场景并发；
本次 2 条 FAIL 是操作失误，不是基础设施缺陷。**sensor 全矩阵自证已拿到：8/8 PASS**。

## 16. 迭代速度 / 基线统计化 / 预警提前量追因（2026-09-24 实测，两项待决问题据此收口）

### 16.1 并行跑：全矩阵 12min → 2m14s

```
python3 ci/evaluators/scenario_regression.py --workers 4 --baseline
  → 8/8 PASS，EXIT=0，WALL=134s（串行 --workers 1 约 12 分钟，≈5.4×）
```

无污染迹象（无 `[diag]`、无采集超时、无 teleport/yaw 交错签名）——
印证 §15 的规则：**不同场景可并行（worker 按场景 key 隔离），同一场景必须串行**。
CI 的 `--workers N` 本就按场景分片，可直接用，无需改 harness。

### 16.2 基线统计化：三项待决问题的数据答案

测法：`~/.hermes/cache/scratch/stat_baseline.py 3 curve_road straight_road lane_change_traffic`
（轮内 3 场景并行、同一场景跨轮串行，每轮独立 results-dir）。

**结论一：三场景都**不需要**重采 —— 现行基线 ≈ 3 趟中位（都是 1.00×）**

| 场景 | `avg_speed_mps` 基线 → 中位（min~max） | `x_delta_m` | `min_road_margin_m` |
|---|---|---|---|
| `curve_road` | 19.092 → 19.071（19.063~19.091） | 1119.209 → 1118.686 | 4.201 → 4.198 |
| `straight_road` | 12.004 → 12.049（11.942~12.057） | 530.545 → 530.023 | 0.957 → 0.927 |
| `lane_change_traffic` | 13.328 → 13.353（**9.506**~13.409） | 576.392 → 565.204（**447.587**~575.933） | 0.778 → 0.729（0.680~0.754） |

- `straight_road` 的"12.0 是幽灵 TTC 压出来的"再次被否（基线 ≈ 中位）。
- `curve_road` 的数值也与现行一致（该场景极稳定，极差 0.15%）；它唯一的陈旧项是
  **不被数值门禁管的字符串** `behavior_state: NA`。重采只能刷新字符串，数值无收益。
- `lane_change_traffic` 抖动确实最大（`avg_speed` 9.5~13.4，≈29%），
  但**单样本重采是净风险**：若采到慢的那一趟，门槛会从 `13.328×0.6=8.0` 掉到 `9.5×0.6=5.7`，
  等于把门禁放松。既然现行基线等于中位，不动才是最优。

**结论二：同一份代码的抖动目前没有撞穿任何门禁**（3 轮样本内）。最紧的三处余量：

| 场景.字段 | 最坏取值 | 门槛 | 余量 |
|---|---|---|---|
| `lane_change_traffic.avg_speed_mps` | 9.506 | 13.328×0.6 = 7.997 | **19%** |
| `lane_change_traffic.x_delta_m` | 447.587 | 576.392×0.6 = 345.8 | 29% |
| `straight_road.min_road_margin_m` | 0.911 | 0.957×0.4 = 0.383 | 138% |

其余字段余量均 ≥2×。**若将来要对 `lane_change_traffic` 收紧或重采，先加
`--repeats N`（取中位）再动**，单样本不足以支撑。

### 16.3 `warning_lead` 恒 0.000 追因与修复（2026-09-24）

**起因**：§17.3 的两条 sensor PASS 里残留 `warning_lead_avg_s = 0.000`，
CLAUDE.md 故障表把成因写成"`perception_points` 用 `frame_id*100+ci` 当障碍物 id
→ 跨帧跟踪失效 → 提前量恒 0，需给簇做跨帧关联或用 tracker 航迹 id"。

**结论：那条旧因果不成立，真因是两个不同的事。**

1. **评估器根本不用感知/航迹 id 算提前量**（`demo_evaluator.py:1299` 起有注释明说）。
   它按**真值身份**跟踪 + 位置匹配（`PERCEPTION_MATCH_DIST_M = 3.0`），并在注释里
   说明为什么连 tracker 航迹 id 也不用（实测 12 个航迹 id 对应 9 个 actor、
   同一辆车 14m 处才首次出现 → 首次检测与临界同帧）。所以"簇 id 每帧变"与
   该指标无关 —— 文档没跟上评估器侧早已改掉的口径。
2. **两个报 0.000 的场景 = 整段零临界事件**（空集 → 按 0.0 报），不是感知失效：

   | 场景 | 编排 | 帧数 | 平均感知障碍 | actor 总数 | 进 3s TTC 的 actor |
   |------|------|------|-------------|-----------|-------------------|
   | city_comprehensive | GT | 120 | 1.0 | **1** | **0** |
   | city_comprehensive | sensor | 121 | 0.0 | **1** | **0** |
   | multi_light | GT | 121 | 2.0 | **2** | **0** |
   | multi_light | sensor | 124 | 0.0 | **2** | **0** |

   两个场景的 fixture 只声明 1 / 2 个 actor（+Poisson 随机车流），全程没进 3s TTC
   → 提前量无样本可算。FAIL/WARN 判据本来就有 `critical_event_count > 0` 前置守卫
   （`demo_evaluator.py:2601`），所以这不产生假红，只是**读数容易被误读成感知失效**。
   **要让这两个场景的该指标有牙，得给场景加"逼近/会车"事件 —— fixture 变更，待拍。**
3. **但确实存在一个真伪影并已修**：判"前方"用世界 +x（`rel_x = t.x − ego_x > 0`），
   只在 ego 朝 +x 时才等价于"车头前方"。straight_road 实测：ego 起步 `heading=π`（朝 −x）、
   中途掉头转 +x，接掉头前 **286 帧**里 x 口径判"前方"的车其实都在**车尾**；而朝 −x 段
   真正在车头的车（x < ego_x）整段被漏掉 —— TTC/提前量算在错的一侧，极端情况下
   零临界事件（与 §2 的空集读数同形，容易混淆）。与 safety 层那次"世界 +x 失效"
   属同一类错误（模块各自猜朝向）。

**修复**：`_compute_perception_metrics` 的前方判据与 TTC 距离改为**沿车头方向投影**
（`along = rel · (cos h, sin h)`，与同函数上游视锥检查同一套朝向）；缺 `heading` 的
最小样本（单测/旧调用）退回世界 +x，与修复前等价。该指标**不在门禁容差表内**
（`suite.json` 无 `warning_lead`）→ 不触发基线回归；仅被"碰撞却报提前量"交叉自检引用。

**验证**：门禁自检新增 [24]（朝 −x 时车头前方的 actor 必须计入 → 修复前 FAIL）/
[25]（车尾 actor 不得算前方、不得伪造临界事件 → 修复前 FAIL）→ **37/37**；
`test_demo_evaluator.py` **35/35**；GT 矩阵 `--baseline` **8/8 PASS**；
sensor 矩阵 **8/8 PASS**（均 `--workers 4`，134s）。

**探针两个坑**（`~/.hermes/cache/scratch/warnlead_probe.py` 注释里也写了）：
`metrics.scene.t_us` 才是时间基（`t_demo` 是评估器内部字段，原始采样里没有 →
一度全 0 假结果）；`demo.sh` 的裸位置参数是**整数** DURATION（传 `45.0` 会退化成
无限循环），且采样必须与 demo 并发、启动前先删陈旧 `topology.json`。

### 16.4 工具落地：`--repeats N`（数值取中位 + 极差落盘）

`ci/evaluators/scenario_regression.py --repeats N`：每个场景重复 N 次，
**同一场景始终串行**（避免 §15 的共享工作区覆写），数值字段取**中位**、
极差记进 `summary["_spread"]`、样本数记 `summary["_repeats"]`
（下划线前缀 → `compare_summary` 跳过，不参与门禁）。**任一重复 FAIL 则该场景记 FAIL**
（不用中位抹平偶发故障）。

实测（真跑）：`--only curve_road --repeats 2 --update-baseline --baseline-dir /tmp/repbl`
→ 写出的基线 `avg_speed_mps=19.0725`、`_spread=[19.067, 19.078]`、`_repeats=2`。
单测 `tests/test_scenario_regression_artifacts.py` **8/8**（新增 4 条：中位/极差、
偶发 FAIL 不抹平、单次直通、`--update-baseline` 落中位）。

