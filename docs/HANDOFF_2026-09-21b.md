# 交接文档 — 2026-09-21b（同日第二 session）

> 接力 `HANDOFF_2026-09-21.md`（同日第一 session）：先做 §7 的 **P0**（perception_node
> 接 `sensor/lidar_points`、删自证 fakery），再继续 §7 的 **P1**（CI gate / 纯逻辑抽离 /
> evaluator 感知指标 / ltv_mpc 验证）。

---

## 0. 一句话总结

- **P0 完成**：自证闭环链路真的通了 —— `sensor_model` 射线投射 → `sensor/lidar_points`
  （`LidarPointCloud` 二进制，车体系）→ `perception_node` DBSCAN → `perception/obstacles`
  → `object_tracker`（实测 `n_dets=1~2`、`tracks=2~5`，无掉线告警）。
- **P1 做了 3.5 项**：msg 线格式 CI gate、3 个模块纯逻辑抽离、evaluator 感知指标接真实
  感知输出、ltv_mpc 验证（结论：**不切默认**，见 §3.5）。
- **默认编排未切换**（`mode=ground_truth`、`lidar_mode=0`、`ltv_mpc_enable` 不加），
  `tests/baseline` 未改数值。

挖出 4 个比原任务更值钱的问题（§3）：`sensor/lidar_points` 的二进制契约冲突、
`msg_codegen` 的 padding 陷阱（20 个消息类型里 12 个尺寸不一致）、`demo_evaluator`
的识别率其实是"真值自比"、以及 LTV MPC 在 lane_change 场景**真撞车**。

---

## 1. 提交图（main 领先 origin/main 10 commits，未推送）

```
（P1 批）
d07e4fc feat(evaluator): 补真实感知质量诊断指标（scene.perceived）
93ca95b feat(ci): msg 线格式/结构体尺寸一致性 gate（钉住 msg_codegen padding 陷阱）
211ea7d refactor(adas_nodes): 抽 pwm_map / sensor_model_weather / slam_math 纯逻辑 + 单测
（P0 批）
a480396 docs(handoff): 2026-09-21b session 交接（P0 consumer 侧接通）
01a1cc2 fix(sensor_model,perception): LidarPointCloud 按线格式收发（msg_codegen padding 陷阱）
b81062e feat(perception): 订阅并消费 sensor/lidar_points，删除自证 fakery
b4f816b feat(perception_points): 抽点云消费纯逻辑 + 单测（反漂移）
4dbb320 fix(sensor_model): sensor/lidar_points 改回 LidarPointCloud 二进制契约
0e244fc docs(handoff): 修正提交数（11→7）                    ← origin/main
```
后续 P1 提交见 §11。

---

## 2. 本次做了什么

### 4dbb320 — producer 契约 + 保真度（`sensor_model_node.c`）

| 项 | 旧 | 新 |
|---|---|---|
| 消息类型 | ad-hoc JSON（`type_id=0`） | `LidarPointCloud` 二进制（40980B，`0x7d34ca5c`） |
| 坐标 | 世界系 `ego + dir*t` | **传感器（车体）系** x 前/y 左/z 上 |
| AABB 尺寸 | 全部硬编码 4.6×2.0 车盒 | 解析 `ol%d/ow%d`，行人 0.5×0.5 就是 0.5×0.5 |
| 射线数 | 64（1.9°/根） | 240（0.5°/根） |
| advertise | 缺失 | 补 `discovery_advertise` + `transport_advertise` |
| `point_count` | 无论 mode 都编数 | `lidar_mode=1` 时用真实命中数 |
| `n_obs` 兜底 | `n<1 → 3`（ego 原点伪造障碍物） | 0 就是 0 |
| 空场景 | — | `count=0` 也发（下游靠它发空 ObstacleList 清跟踪） |

密度换算：DBSCAN `min_pts=4`、`eps=2m` 下，0.5m 行人 ~14m 内可成簇、2.0m 车宽
~57m 内可成簇。64 根时行人在 5m 外连 4 个点都打不出来 —— 等于必然漏检。

### b4f816b — 纯逻辑抽离 + 单测（`perception_points.{h,c}`）

照 `imu_protocol` 样板：节点与测试链接同一份实现（不再"副本 + 行号标注"）。
- `perception_points_from_cloud()`：点云 → DBSCAN 输入，守卫非有限值/量程/近场；
  `count` 超契约容量整帧拒绝（不静默截断）。`PERCEPTION_MAX_CLOUD_POINTS` 用
  `_Static_assert` 对齐 `LidarPointCloud.points[]` 容量。
- `perception_clusters_to_obstacles()`：簇 → `ObstacleList`（lane_id 反算 + 类型映射）。
- `dbscan_set_ground_mode()`：新增 API（见 §3.3）。
- 单测 +9 例，42/42 通过。

### b81062e — consumer（`perception_node.cpp`）

- 新增 `sensor/lidar_points` 订阅：回调在总线线程、`run()` 在线程池 →
  `pthread_mutex_t` + `cloud_seq` 换缓冲（同 `slam_node.cpp` 惯例）。以 `cloud_seq`
  是否推进判断"本帧有没有新点云"，没有就跳过（50 帧节流 warn），不拿旧点云凑。
- 删掉 fakery：24 地面环、8×N 障碍物表面点、FOV/近邻 beam 遮挡，以及随之失去
  调用者的 `obstacle_in_fov()` / `rand_uniform_signed()`。
- `mode=0`（默认）不再构造假点云跑 DBSCAN —— 那趟聚类结果本来就被 ground_truth
  分支丢弃。顺带修掉：旧分支条件 `mode==0 && n_obs>0`，空场景会掉进 else 用假点云
  的聚类结果。
- 建筑 LOS 过滤保留，改为作用在**收到的真实点云**上（producer 不投射 buildings）。
- ego 位姿：sensor 模式取 `sensor/lidar` 的带噪定位估计，缺失时退回真值；点云本身
  不含位姿，障碍物位置不泄漏定位真值。
- config 5 份：perception 订阅补 `sensor/lidar_points`；cortex/manual 的 sensor_model
  补发布（否则 publisher-existence 检查会判悬挂订阅，它用的是 `json_in | code_in`）。

### 01a1cc2 — 线格式修复（见 §3.2）

---

## 3. 本次挖出的关键问题

### 3.1 `sensor/lidar_points` 早有二进制契约，JSON 是冲突的

`msg/adas_msgs.msg:59-64` 定义了 `LidarPointCloud`（2048 点、含 `time_offset_us`，
给 deskew 用），`slam_node.cpp:543` 就是按它 `LidarPointCloud_deserialize` 的。
上一 session 的 e86ea5b 却在这条 topic 上发 ad-hoc JSON —— slam 收到必然解失败，
`config/pipeline.json` 声明的 `"type":"JSON"` 也是错的。已改回二进制（§2 表）。
**教训**：新增 producer 前先 grep topic 名，看有没有既有消息契约/消费者。

### 3.2 `msg_codegen` 的 padding 陷阱 —— 20 个类型里 12 个尺寸不一致 ⚠️ 最大发现

生成器的 `_struct_size()` 是**字段尺寸直接相加**（= 期望的线格式尺寸），但生成的结构体
**没有 `__attribute__((packed))`**。只要字段排列产生对齐填充，两者就不等：

```
LidarPointCloud   声明 40980 / sizeof 40984      ObstacleList  4368 / 4632
PredictionSet     3068 / 3088                    Trajectory    2581 / 2592
PredictionHypothesis 1017 / 1020                 Localization  57 / 60
GpsData           36 / 40                        ImuData       36 / 40
Obstacle          34 / 36                        Pose2D        29 / 32
Behavior          22 / 32                        ControlRaw    59 / 60
（审计脚本：解析 build/gen/*.h 的 "size=NB" 注释 vs sizeof，见 §6）
```

于是仓库里并存两套互不兼容的约定：

| 约定 | 语义 | 代表用法 |
|---|---|---|
| 线格式 | 紧密排布，`*_serialize`/`*_deserialize` | perception 发 `ObstacleList`、slam 收点云 |
| 内存布局 | 自然对齐，`msg_cast`/`msg_init_typed(&T, sizeof(T))` | `_msg_cast_impl` 的 legacy 分支就是按 `sizeof` 比对 `data_size` |

同一 topic 的生产者/消费者**必须成对使用同一套**，否则静默错位。今天各 topic 靠
约定侥幸一致；`LidarPointCloud` 是第一个踩中的（无人用过这个类型）。

本次的踩坑现场（两处都已在 01a1cc2 修）：
1. producer 用 `msg_init_typed(..., &cloud, sizeof(cloud))` 发内存布局 → 对端按线
   格式读 `count`，读到的其实是 `timestamp_us` 的高 32 位。仿真时钟 <2³² µs 时高
   32 位恒 0 → `count=0` → 点云静默为空（无任何错误日志）。
2. consumer 用 `msg->data_size < sizeof(LidarPointCloud)` 当长度门槛 → 线格式
   40980 < 40984，每帧都被当"太短"丢掉（表现为 "no fresh sensor/lidar_points" 刷屏）。

**根治建议（需要单独决策，本 session 不动）**：codegen 给结构体加
`__attribute__((packed))`（或让 `_struct_size` 计对齐）。代价：12 个类型的 ABI 变化，
可能影响录制回放（`pem`/`mcap`/`bag`）、`flowmond` 前端与 `tests/baseline`。
建议顺序：先加一个"每个生成类型 `sizeof == 声明 size`"的 CI gate 暴露现状 →
再评估是否 pack。**注意 pack 会让 `uint64` 落在非对齐偏移**，ARM 上取值需
`memcpy` 语义（x86 无碍）。

### 3.3 DBSCAN 的 RANSAC 地面去除在 z=0 点云上"点数相关"地失效

真点云只有障碍物命中点、没有地面回波（z 恒 0）。`ransac_fit_ground()` 会把 z=0
拟合出来、命中 100% 内点 → 整帧清空 → 0 聚类。但它的候选平面预筛要求
`quick_count >= 20`，而采样只在 `min(50, n)` 个点上做：

- `n >= 20`：真的会拟合出 z=0 并把点全清掉；
- `n < 20`（含 `n < 10` 直接返回 -1）：地面去除**静默失效**，点活下来。

即同一辆车 8m 处（约 30 点）会消失、30m 处（约 8 点）反而可见 —— "看不见近处、
看得见远处"。所以 sensor 模式改用 `GROUND_REMOVE_NONE`（`dbscan_set_ground_mode`），
并留了回归用例 `test_perception_ground_remove_none_rationale`。
**代价**：这条路径不再做地面分割；将来 producer 补 elevation/地面回波时应重新评估。

### 3.4 `demo_evaluator` 的识别率/预警指标是"真值自比"，测不出感知

`_compute_perception_metrics()` 用的是 `series[i]["obs_world"]`，而它来自
`scene.obstacles`，`monitor_node.c:1530` 是从 **`vehicle/state` 真值** 构造的
（`perception/obstacles` 只进 `g.latest_obstacles_json`，没进 scene）。

实测对照：`urban_challenge` 的 baseline（ground_truth）与本次 sensor 模式跑出来的
`recognition_rate_vru/vehicle = 1.000`、`perceived_track_count = 9` **完全一致**。
所以：

- `recognition_rate_*` / `warning_lead_*` / `perception dropout` 目前**不能**用来
  判断感知链路好坏；
- 要真正评估 sensor 模式，得先把 `scene.obstacles` 换成 `perception/obstacles`
  （或让 evaluator 用 monitor 的 `latest_obstacles_json`）。这是一个独立且有价值的
  P1：现在 CI 的"感知 100%"是自证。

---

## 4. 验证状态

| 项 | 结果 |
|----|------|
| 顶层构建 | ✅ 通过（仅既有 warning） |
| `modules/adas_nodes` 构建 | ✅ 通过 |
| ctest 顶层 | ✅ 29/29 |
| ctest adas_nodes | ✅ 7/7 |
| `test_adas_nodes_logic` | ✅ 42/42（含 9 个新用例） |
| `topic_contract_check` | ✅ 5/5 configs |
| `zombie_ban_check` | ✅ OK |
| `scenarioctl validate` | ✅ OK |
| `scenario_regression --baseline` | ✅ 无数值回归（全部 `regress "-"`） |
| **sensor 模式 demo**（urban_challenge 16s） | ✅ hits 4~162 → `np` 同步 → tracker `n_dets=1~2`/`tracks=2~5`；无 "no fresh"、无 DBSCAN overrun |
| **sensor 模式完整场景评估**（urban_challenge） | ✅ PASS，无 perception dropout |

**行为门禁的已知抖动（非本次引入）**：`--baseline` 三次全量跑都是 7/8 PASS，但
失败场景在换 —— 第 1 次 `lane_change_traffic`（`min_forward_gap 3.28m` 低于
desired 15.9m），第 2、3 次都是 `curve_road`（`duration 61.2s / 61.1s > 60.0s`，
上一 session 记录的同一个墙钟噪声）。同一二进制单跑 `lane_change_traffic` 为
PASS（`min_forward_gap 13.05m`），改前版本单跑也出现过 8.64m 的临界值 —— 属
§5.3 已知的 flowsim 非确定性（墙钟 pacing + 进程级 `rand()`）。**三次跑都没有
任何数值回归**（`regress` 列全为 `-`），即改动没有把任何指标推出容差带。

---

## 5. 未解决 / 已知遗留

### 5.1 本次新增（按价值排序）

| 优先级 | 项 | 备注 |
|---|---|---|
| **P0** | `msg_codegen` padding 陷阱（§3.2） | 12/20 类型尺寸不一致；先加 CI gate，再决定是否 pack；会动 ABI 与 baseline |
| **P1** | evaluator 感知指标改吃 `perception/obstacles`（§3.4） | 否则"识别率"永远是真值自比，sensor/ground_truth 无差别 |
| **P1** | sensor 模式切默认 + 重生成 baseline | 前置：producer 补 `world/buildings` OBB 遮挡；决策 `lidar_max_range_m`（场景 actors 多在 56~90m，60m 量程导致前 10s 检不到）与射线数（行人 ~14m/车 ~57m） |
| **P1** | producer 补 elevation 维度（z 恒 0） | 补上后 consumer 才能恢复地面分割，也才能做 3D 分类 |
| **P1** | 障碍物朝向：`vehicle/state` 不发布各障碍物 heading | 现在 AABB 近似"与 ego 同向"，横穿/掉头目标会失真 |
| P2 | 抽剩余纯逻辑（slam / pwm / sensor_model / safety_control） | 沿用 `imu_protocol` / `perception_points` 样板 |
| P2 | `ltv_mpc` 启用 `use_ltv_mpc=1` | P1 #4 修完 math bug 后小流量验证再切 |

### 5.2 沿用上一份 handoff

规划 W1（`d_out` 无条件覆盖）/ W3（碰撞点测非扫掠）/ W4（无预测建模）/ W5（FSM 死状态）、
`hw` profile 结构性损坏、`min_road_margin` 漂移（结论：测量窗口切换，非回归）。

### 5.3 已知限制（设计/约定，非 bug）

1. `~/.claude/skills/*` 在 CLAUDE.md 里被引用但本机不存在
2. flowsim 非确定性：墙钟 pacing + 进程级 `rand()` → 同 seed 不保证复现同一世界
   （`curve_road` duration 60.0~61.2s、`lane_change_traffic` min_forward_gap 3~16m）
3. BEV 影子模式：`bev/obstacles` 零订阅者，`ENABLE_ONNX=OFF`
4. `st_graph.c` / `control_node.cpp` / `behavior_planner_node.cpp` / `object_tracker_node.c` 仍零单测
5. `libflowmond_node.so` 缺 `-lz`

---

## 6. 验证命令（接手后跑这套就能复现状态）

```bash
# 构建
cmake --build build -j$(nproc)
cmake --build build/modules/adas_nodes -j$(nproc)

# 测试 / 门禁
ctest --test-dir build --timeout 60 -LE "benchmark|manual|stability|integration"   # 29/29
ctest --test-dir build/modules/adas_nodes --timeout 60                            # 7/7
env -u LD_LIBRARY_PATH build/bin/test_adas_nodes_logic                            # 42/42
python3 ci/gates/topic_contract_check.py && python3 ci/gates/zombie_ban_check.py
python3 tools/scenarioctl.py validate
python3 ci/evaluators/scenario_regression.py --workers 1 --baseline
```

**sensor 模式端到端**（临时改 `config/pipeline.json`，跑完 `git checkout` 还原）：

```bash
cp config/pipeline.json /tmp/pipeline.json.bak
python3 - <<'EOF'
import json
cfg = json.load(open('config/pipeline.json'))
for p in cfg['processes']:
    if p['name'] == 'sensor_model':
        par = json.loads(p['params']); par['lidar_mode'] = 1; p['params'] = json.dumps(par)
    if p['name'] == 'perception':
        par = json.loads(p['params']); par['mode'] = 'sensor'; p['params'] = json.dumps(par)
json.dump(cfg, open('config/pipeline.json','w'), indent=2)
EOF
bash scripts/demo.sh 16 --no-browser --skip-services --scenario scenarios/urban_challenge.json
grep -E "n_dets=[1-9]" /tmp/flow_logs/tracker.log | tail -3   # 期望：追踪器收到真实检测
grep -c "no fresh" /tmp/flow_logs/perception.log              # 期望：0
cp /tmp/pipeline.json.bak config/pipeline.json

# 消息类型线格式审计（§3.2 的证据复现）
python3 - <<'EOF'
import re, glob
for h in sorted(glob.glob('build/gen/*.h')):
    s = open(h).read()
    m = re.search(r'/\* Struct: (\w+) \(size=(\d+)B', s)
    if m: print(m.group(1), m.group(2))
EOF
# 再与 sizeof 对比：把上面的类型名喂进一个 #include "adas_msgs_gen.h" 的小程序即可
```

---

## 7. 建议下一步

| 优先级 | 动作 | 备注 |
|--------|------|------|
| P0 | **codegen 尺寸一致性 CI gate**（先暴露，不 pack） | 遍历 `build/gen/*.h`：`sizeof(T) == 声明 size`，12 个类型当前会红 → 明确"这 12 个只能用线格式收发" |
| P1 | **evaluator 感知指标接 `perception/obstacles`** | 让 CI 真正能测感知；不然 sensor 模式切换与否指标都一样 |
| P1 | producer 补 `world/buildings` OBB 遮挡 + elevation | 观测模型完整性 |
| P1 | sensor 模式切默认（含 baseline 重生成） | 前置：上面两项 + 量程/射线数策略 |
| P2 | 抽剩余 4 个模块纯逻辑 | `imu_protocol` / `perception_points` 样板已就绪 |

---

## 8. 不要做的事

1. **不要**在 `sensor/lidar_points` 上发 JSON/其它类型 —— 二进制契约归 `LidarPointCloud`
2. **不要**用 `msg_init_typed(&struct, sizeof(struct))` 发 §3.2 表里那 12 个类型；
   要么用 `*_serialize`，要么确认对端也是 `msg_cast`/`sizeof` 约定
3. **不要**用 `sizeof(T)` 当 wire-size 门槛（对上面 12 个类型不等）
4. **不要**在 sensor 模式给 DBSCAN 开 RANSAC 地面去除（z=0 点云会被清空或静默失效）
5. **不要**在 `cd modules/adas_nodes/build` 里构建（stray 目录）
6. **不要**相信手写的 `docs/PIPELINE_ARCHITECTURE.md`（DEPRECATED），真拓扑看
   `config/pipeline.json` 的 `processes[]`

---

## 9. 文件索引（本次动过）

```
modules/adas_nodes/sensor_model_node.c         4dbb320 + 01a1cc2
modules/adas_nodes/perception_node.cpp         b81062e + 01a1cc2
modules/adas_nodes/perception_points.h         b4f816b（新）
modules/adas_nodes/perception_points.c         b4f816b（新）
modules/adas_nodes/CMakeLists.txt              b4f816b（加源 + -fcoroutines 只给 CXX）
src/algorithms/dbscan_cluster.h                b4f816b（+ dbscan_set_ground_mode）
src/algorithms/dbscan_cluster.c                b4f816b
tests/test_adas_nodes_logic.c                  b4f816b（+9 用例）
CMakeLists.txt                                 b4f816b（测试目标加 perception_points.c）
config/pipeline.json                           b81062e（订阅）+ 01a1cc2 前（type）
config/pipeline_windows.json                   b81062e（订阅）+ 01a1cc2 前（type）
config/pipeline_cortex.json                    b81062e（订阅 + 发布）
config/pipeline_manual.json                    b81062e（订阅 + 发布）
docs/HANDOFF_2026-09-21b.md                    本文件（新）
```

---

## 10. 一句话给下一位接手的人

自证闭环的 consumer 侧已接通并端到端验证（sensor 模式 demo 与场景评估都过，默认
编排与 baseline 分毫未动）。但真正最该先做的是 §3.2：`msg_codegen` 有 12/20 个类型
的结构体尺寸 ≠ 线格式尺寸，仓库里并存"线格式"和"内存布局"两套收发约定 —— 这次
LidarPointCloud 踩中，代价是整整两轮"点云静默为空"的排查。先加 CI gate 把它钉住。

---

## 11. 追加：P1 批（同日续）

§10 那段"一句话"里的 CI gate 已经落地了。下面是 §7 P1 的执行结果。

### 11.1 `msg_codegen` 尺寸一致性 CI gate ✅ `93ca95b`

新增 `ci/gates/msg_layout_check.py` + `.github/workflows/ci.yml` 的 `msg-layout-gate` job：

- 线格式尺寸直接复用 `tools/msg_codegen.py` 的 `CodeGenerator._struct_size()`（不与生成器漂移）
- C `sizeof` 用 Python 按对齐规则模拟（成员各自对齐 / 结构体对齐取最大 / 总尺寸向上取整），
  已与真实编译器逐类型核对一致（20/20 同值）
- 对照 `KNOWN_MISMATCHES`（当前 12/20，含数值）：新出现、数值变动、清单过期三类都报错
- 纯静态、秒级、不依赖编译产物。自测三种漂移均 exit 1

**根治未做**（加 pack 会动 12 个类型的 ABI：录制回放 / dashboard / baseline），留给单独决策。

### 11.2 纯逻辑抽离 3/4 ✅ `211ea7d`

- `pwm_map.{h,c}`（ControlCmd→PWM，含钳位）
- `sensor_model_weather.{h,c}`（可见度/天气→衰减）
- `slam_math.{h,c}`：`slam_wrap_pi()`（原 `ekf_slam.c` 里**内联了 4 遍**的航向归一化，
  改用 `fmodf`：大幅值不再转很多圈、NaN 不再死循环）+ `slam_dry_run_pose()`

顺带抓到一个测试漂移实例：测试里的 `normalize_heading_pi` 副本抄的是 `slam_node.c` 里
**已被 EKF 重构删掉**的死代码 —— 也就是说那条用例一直在测一个不存在的函数。

**未做**：`safety_control_node.cpp::arbitrate_control`。它的 `ControlCmd` 是节点私有的
C++ struct（含 `std::string mode`），抽公共 C 接口要先改数据结构，属安全关键路径的数据
类型重构，建议单独一个 commit 做。

### 11.3 evaluator 感知指标：先补"真实感知"诊断，未直接换成门禁 ⚠️

结论先说：**旧的 `recognition_rate_*` 确实是"真值自比"**（`obs_world` 来自
`monitor_node.c` 里的 `vehicle/state` 快照），已确认无法反映感知。但把它直接换成感知
输出会让 CI 变红在**度量伪影**上，所以本次做成"诊断指标 + 门禁不动"。

三种候选来源的实测（`lane_change_traffic`，默认 ground_truth 模式）：

| 来源 | 含义 | vehicle | vru | 问题 |
|---|---|---|---|---|
| `scene.obstacles`（旧） | vehicle/state 真值 | 1.000 | 1.000 | 真值自比，测不出感知 |
| `scene.perception_entities` | object_tracker 已确认航迹（世界系） | 0.536 | **0.000** | 叠加 KF 确认门限/静态动态分类；实测该场景行人从未进 perceived（把跟踪器行为记到感知头上）|
| `scene.perceived`（新） | `perception/obstacles` 原始输出 | 0.846 | 0.974 | 车体系点 + 每帧递增 id；monitor 用"当前" ego 位姿反算世界系 → ~1 个感知周期的位姿滞后（20m/s 下米级），识别率被系统性压低；预警提前量需要稳定 id 才能跨帧跟踪，per-frame id 会让 lead 恒 0 |

处置分两步（`modules/adas_nodes/monitor_node.c` + `perception_node.cpp` +
`ci/evaluators/demo_evaluator.py`）：

**第一步（诊断）**：monitor 把 `perception/obstacles` **反序列化**（顺带替掉那个 memcpy
后从未被读的死缓存 `latest_obstacles_json`），输出 `scene.perceived`（世界 ENU）；
evaluator 加一路诊断指标，先把真实感知"看得见"。

**第二步（正式落地，见 §14）**：补掉两个度量伪影后，`_compute_perception_metrics()`
的 perceived 源**直接换成 `scene.perceived`**（识别率/dropout 检查都变成真实感知）。两个伪影是：

| 伪影 | 现象 | 修法 |
|---|---|---|
| 位姿滞后 | 车体坐标系障碍物 + monitor 用"最新" ego 位姿反算世界系 → 一个感知周期（50ms×20m/s＝米级）的系统性偏移，ground_truth 直通也只 0.849 | `perception_node` 给 `ObstacleList.timestamp_us` 打戳；monitor 维护 64 帧 ego 位姿历史，按时间戳就近配对（差值 >500ms 才退回最新，并计数 `perceived_pose_fallback`）→ 0.849 **→ 0.997** |
| id 不稳 | `perception` 原用 `frame_id*100+i` 当障碍物 id → 每帧都变，预警提前量（按 id 跨帧跟踪）恒 0，且会伪造 critical event | `perception` 解析并使用 `vehicle/state` 的 `oid%d`（flowsim 实体 id，跨帧稳定），缺失才退回帧内 id |

### 11.4 LTV MPC：验证后**不切默认** ❌（结论性）

按 §7 P1「小流量验证再切默认」执行：把 `config/pipeline.json` 的 control 参数加
`ltv_mpc_enable: 1`，跑全量 8 场景 + 单跑 3 次。结果：

```
lane_change_traffic  FAIL（3/3 次复现）
   - collision detected: topic_pub=1
   - flowsim invariant failed: total=4（spatial+motion+temporal）
   - lateral excursion too large: y range=6.43m > 4.5m（y_min=-4.19 y_max=2.24）
   - comfort_jerk_max = 213 m/s³（默认 Stanley 下约 9）
其余 7 个场景 PASS；三次全量跑 regress 列全为 "-"（无数值回归）
```

即 2026-08-15 那条"未经 demo_evaluator 充分回归前不作为默认"的裁决现在有了**充分回归**，
而结论是**不能切**：MPC 在 lane_change 场景会横向来回甩（跨到对向车道）并触发碰撞。
默认保持 Stanley（`ltv_mpc_enable` 不写入 config）。**下一步要动的是 MPC 本身**：
`lane_change` 下的横向阻尼/权重（`control.ltv_q_y` / `ltv_q_psi` / `mpc_max_dsteer`）
需要重新标定，并加一条"横向不越线（|d| 不超过车道宽）"的硬约束或后置校验。

### 11.5 判定为"不必做"的一项：producer 侧 `world/buildings` OBB 射线投射

§7 P1 说"producer 缺建筑遮挡，复用 perception 的 OBB 缓存"。实际核对后：**consumer 侧
已经补上了同一件事** —— `perception_node` 对收到的每个点做 ego→点 的 LOS 线段与建筑
OBB 求交，穿过建筑的点直接丢弃。producer 侧再投一次的建筑遮挡在**可观测结果上等价**
（唯一的差别是"射线该停在墙上并返回墙面点"，而我们两边都没模拟墙面回波）。
所以这条不作为待办，除非将来要模拟建筑墙面回波。

---

## 12. P1 剩余项与需要你决策的一项

| 优先级 | 项 | 说明 |
|--------|----|------|
| **P1（需决策）** | **规划 W1：车道内避障** | 现状：`planning_node.cpp:2172` 巡航时把 `d_out[i]` 无条件写成 `target_lane_offset`（`frenet_bridge.cpp:49` 的 `kd=0` 配合它），Frenet 的横向避障输出被丢弃 → 面对本车道障碍只有纵向 TTC 跟车/刹停。三个选项见下 |
| ~~P1~~ ✅ | ~~evaluator 感知指标升级为门禁~~ | 已完成，见 §14 |
| P1 | `safety_control` 抽纯逻辑 | 需先改节点私有 ControlCmd 的数据结构 |
| P1 | `use_ltv_mpc` 重新标定后再评估 | 见 §11.4 |
| P1 | sensor 模式切默认 + baseline 重生成 | 前置：producer 补 elevation / 建筑遮挡已等价覆盖；量程（场景 actors 多在 56~90m，60m 量程导致前 10s 检不到）与射线数策略要定 |

### W1 的三个选项（请选一个，或给别的方向）

- **(a) 架构正解**：Frenet 参考线改成"目标车道中心线"（按每点法向平移 `map_ref`），
  恢复 `kd>0`，删掉 `d_out` 硬覆盖 → 由 FOT 的代价函数自己权衡"贴车道中心 vs 避障"，
  `is_valid_path` 兜底不撞。影响：所有场景横向行为变化 + baseline 重生成，变道逻辑
  （`planning_node.cpp:2089-2132` 的 `d_out` 插值）要对齐到新坐标系。**工作量最大、最正确**
- **(b) 局部绕行**：保留 `d_out` 硬覆盖，只在"本车道前方有低速/静止障碍且相邻车道有空间"
  时叠加一段横向偏移（复用变道那套 `d_out` 插值）。改动小，但绕行策略是启发式的
- **(c) 判定为设计选择**：Level-2 场景下"本车道有障碍 → 跟车/刹停"比"自动绕行"更保守安全，
  把这条从缺口降级为文档里的显式约定（需要在 docs 里写明，并去掉 handoff 里的"缺口"定性）

---

## 13. 本批验证状态

| 项 | 结果 |
|----|------|
| 顶层构建 / adas_nodes 构建 | ✅ 通过（无新增 warning） |
| ctest 顶层 | ✅ 29/29（含 ekf_slam_tests、demo_evaluator_tools 22 例） |
| ctest adas_nodes | ✅ 7/7 |
| `test_adas_nodes_logic` | ✅ 42/42 |
| 四个 gate（topic / msg-layout / zombie / scenarioctl） | ✅ 全 OK |
| `scenario_regression --baseline`（P1 批后，默认编排） | ✅ **8/8 PASS，regress 列全 `-`**（本次 session 唯一一次全绿；§14 改动后又跑两次，见 §14.4 的抖动说明） |
| ltv_mpc 验证 | ❌ lane_change_traffic 碰撞（3/3 复现）→ 不切默认 |
| 新诊断指标是否真的反映感知 | ✅ 见下 |

### 13.1 新诊断指标把 sensor 模式的差距量化出来了

同一个 `urban_challenge`（45s），只切 `lidar_mode`/`perception.mode`：

| 指标 | ground_truth（默认） | sensor（真点云） |
|---|---|---|
| `recognition_rate_overall`（**旧，真值自比**） | 1.000 | 1.000 ← 完全无差别，证明旧指标是瞎的 |
| `perceived_recognition_rate_overall`（新，真实感知） | 0.849 | **0.066** |
| `perception_coverage`（真值有障碍帧里感知也有输出的比例） | 1.000 | **0.502** |
| `perceived_count_avg`（每帧平均感知障碍数） | 42.0 | **0.68** |

两个直接结论：
1. **旧指标确实测不出感知**（同一场景两种编排给出一模一样的 1.000）—— §3.4 的怀疑成立；
2. **sensor 模式离可用还很远**：一半真值有障碍的帧感知完全没有输出，逐帧平均只看到 0.68 个
   目标（ground_truth 直通是 42 个）。原因是实打实的物理限制：`lidar_max_range_m=60` 而
   urban_challenge 的 actors 前 10s 多在 56~90m，240 根射线/120°FOV 下 2m 宽的车要 ~57m 内、
   0.5m 行人要 ~14m 内才凑够 DBSCAN 的 4 个点。**切默认之前必须先决定量程/射线数策略**（§12）。
   注意：sensor 模式这场仍然 PASS 了行为门禁 —— 因为控制/规划有真值兜底在跑，
   即"CI 现在也拦不住这种感知退化"，这正是 §11.3 要把诊断指标升级成门禁的理由。

---

## 14. 追加（同日续 2）：真实感知指标正式进 CI

§11.3 说"先诊断、暂不进 CI"，理由是两个度量伪影。这两个都补掉了，指标已经**正式切换**。

### 14.1 改了什么

| 文件 | 改动 |
|---|---|
| `perception_node.cpp` | `ObstacleList.timestamp_us = clock_now_us()`（原来留 0）；mode=0 用 `vehicle/state` 的 `oid%d`（flowsim 实体 id）当障碍物 id（原来 `frame_id*100+i`，每帧都变） |
| `monitor_node.c` | 64 帧 ego **位姿历史**（`ego_pose_at()` 按时间戳就近配对，>500ms 才退回最新并记 `perceived_pose_fallback`）；`scene.perceived` 用它做车体系→世界系换算；场景里输出 `perceived_pose_fallback` 计数 |
| `demo_evaluator.py` | `_compute_perception_metrics()` 的 perceived 源正式换成 `scene.perceived`；新增 `perception_coverage` / `perceived_count_avg` / `perception_metric_source`；感知 dropout 检查改为"真值有、感知没有" |
| `test_demo_evaluator.py` | 三个感知用例补 `perceived_world`；新增 `test_recognition_uses_perception_output_not_truth`（真值有+感知空 → 0） |

### 14.2 每一步的实测效果（同一 lane_change_traffic，ground_truth 模式）

| 阶段 | recall(vehicle/vru/overall) | 说明 |
|---|---|---|
| 直接换源（未修伪影） | 0.846 / 0.974 / 0.849 | 位姿滞后 + id 不稳；预警提前量恒 0，还伪造出 26 个 critical event |
| + 位姿时刻对齐 | 0.998 / 1.000 / **0.997** | 伪影消掉，指标可信 |
| + 稳定 id | 0.997 / 1.000 / 0.997 | 预警提前量恢复可算（不再伪造 critical event） |
| 行为门禁 | PASS | 8/8 场景全 PASS |

sensor 模式（同一个 urban_challenge）：`recognition_rate_overall = 0.066`、
`perception_coverage = 0.502` —— 现在 **CI 会真的报感知退化**，而不再是两种编排都给 1.000。

### 14.3 一个必须写下来的观察：这不是"顺手修完"

位姿滞后这条伪影值得单独记一笔：它让"感知直通真值"的场景也只能得 0.849，并且**伪造了
26 个 critical event**（滞后使 `rel_x` 偏小 → TTC 偏小）。也就是说**度量链路自己也会
系统性说谎**，而且是往"看起来更危险"的方向说 —— 如果当初直接把换源当门禁（不做时刻对齐），
就会得到一个"永远差 0.15 且偶尔报假 critical"的门禁，然后大概率被人加白名单绕过。
先量化伪影、再改门禁，顺序不能反。

### 14.4 验证状态（§14 改动）

| 项 | 结果 |
|----|------|
| 顶层 ctest | ✅ 29/29（demo_evaluator_tools 22 例） |
| `scenario_regression --baseline`（§14 改动后） | 7/8：`multi_light` 报 **"no topology samples collected"**（monitor 没在窗口内写出第一份快照 = 基础设施抖动，单跑 2/2 PASS 复现不了）；其余 7 场景 PASS 且 `regress` 全 `-` |
| 同批早前两次全量（同一构建语义） | 一次 **8/8 全绿**；一次 `city_comprehensive` 报 `stagnation_duration_s 7.278 > 6.509`（该指标只看 ego 速度样本，与本次改动无关；单跑 3/3 PASS） |
| 感知指标是否真的联动 | ✅ ground_truth 0.997 vs sensor 0.066（§13.1 / §14.2） |

这两条抖动都不是本次改动引入的（`stagnation_duration_s` 只看 ego 速度；"no topology samples"
是 monitor 写文件超时），但**值得记一笔**：全套 `--workers 1` 会连续跑 8 场仿真，
环境一抖就会给出"看似回归"的假信号 —— 判断时先单跑复现，再谈改动。

### 14.5 未做（留给下一个 session）

- `perceived_pose_fallback` 只做了输出与计数，**没有门禁**（超过阈值该 WARN 还是 FAIL 需要看长时间分布）
- sensor 模式的 `recognition_rate_*` 现在会 FAIL（0.066 < 50% 阈值）——**这是对的**，但它意味着
  "谁把默认编排切成 sensor，CI 立刻红"，切默认之前必须先解决量程/射线数（§12）
- `baseline` 里没有 `perception_coverage` / `perceived_count_avg` 这两个新键（它们只进报告、
  没进 `BASELINE_KEYS`）；要长期跟踪就得 `--update-baseline` 重生成

---

## 15. 追加（同日续 3）：`safety_control` 抽纯逻辑（P1 最后一项）

### 15.1 改了什么

新增 `modules/adas_nodes/safety_arbiter.{h,c}`：把 `safety_control_node.cpp` 的
`arbitrate_control`（规则主干 vs 端到端模型的仲裁）抽成纯 C 逻辑，节点与
`tests/test_adas_nodes_logic.c` 共用同一份实现（同 imu_protocol / perception_points /
pwm_map / sensor_model_weather / slam_math 样板）。

- 接口用普通 C 结构体 `SafetyArbiterCmd`（throttle/brake/steer/turn_signal/hazard/gear），
  **节点私有的 `ControlCmd`（含 `std::string mode`）不动** —— 这正是它当初没被抽出来的原因；
  现在节点侧只做一次字段映射（`speed`/`target`/`error` 仍从 rule_cmd 继承，
  `mode` 由返回的 intervened 标志在节点侧拼 `"ARBITER[...]"`）。
- 三个魔数收口成头文件常量：`SAFETY_ARBITER_STEER_ENVELOPE_RAD 0.12` /
  `SAFETY_ARBITER_RULE_BRAKE_ACTIVE 0.10` / `SAFETY_ARBITER_MODEL_THROTTLE_CAP 0.85`。
- `std::fabs/std::max/std::min` → `fabs/fmax/fmin`，语义逐位一致；测试里那套
  `TestCmd` + `test_arbitrate_control` 副本删掉，5 个用例直接打真实现。
- 顺带给 `safety_control_node` 加 `$<COMPILE_LANGUAGE:CXX>` 守卫（纯 C 源不再吃
  `-fcoroutines` 警告），与 perception_node 一致。

这样 handoff §7 P1「抽出剩余 4 个模块的纯逻辑」**4/4 完成**（imu / pwm / sensor_model /
slam / safety_control，共 5 份实现）。

### 15.2 为什么这条"最后做"

它不是难度问题，而是**风险顺序**问题：仲裁是安全关键路径，任何"顺手重构"都必须能
证明行为逐位不变。做法上刻意选了侵入性最小的一条：不改节点数据结构、不改调用点签名
（`arbitrate_control` 名字与签名保留，内部转调），测试直接换成真实现 —— 这样 diff 里
"行为"只可能出现在映射层，而映射层有 5 个既有用例覆盖。验证见 §16。

### 16. 验证（§15）

| 项 | 结果 |
|----|------|
| 顶层构建 / adas_nodes 构建 | ✅ 通过（无新增 warning；顺带消掉 `-fcoroutines` 对纯 C 源的警告） |
| `test_adas_nodes_logic` | ✅ 42/42（5 个仲裁用例现在打真实现） |
| ctest 顶层 / adas_nodes | ✅ 29/29 / 7/7 |
| `scenario_regression --baseline` | ✅ **8/8 全 PASS，regress 列全空**（抽离对行为零影响，符合预期） |

至此 handoff §7 的 **P1 全部结案**：4 项完成（点云消费、3+2 个模块纯逻辑、感知指标进 CI、
MPC 以"不达标"结案）、1 项判定冗余（建筑遮挡）。**只剩 W1 等你选方向，P2 三项未动**。

---

## 17. 追加（同日续 4）：W3 定性（点测漏检薄障碍）+ 一个新 gate

### 17.1 W3 先量化：结论是"理论成立、当前场景没发生"

新增 `modules/adas_nodes/traj_safety.{h,c}`（纯逻辑 + 4 个单测）：

- `traj_swept_check()`：相邻轨迹点线段 vs 障碍物 AABB（slab 法）扫掠求交。
  采样间距多粗都不会漏 —— 这正是 Frenet 内部"点测"会给不出的保证。
- `traj_point_test_hits()`：复刻 Frenet 的点测，作为**对照组**。单测里那条
  「0.5m 薄墙落在两个 4m 间隔采样点之间」的用例，点测 = 0、扫掠 = 1，
  就是 W3 的定义本身（可执行形式的证据）。
- 单测另覆盖：横向错开不误报、退化盒/NULL 安全、多段多障碍物按对计数。46/46 通过。

规划节点在**发布轨迹**上做一次扫掠检查（`planning_node.cpp` 的 `publish_trajectory` 段），
只统计 + 限频 WARN，**不改行为**；计数进 `planning/debug` JSON：
`traj_swept_check_frames`（真跑过的帧）/ `traj_swept_hit_frames` / `traj_swept_hits`。

实测（红绿灯薄墙场景）：

| 场景 | planning 帧 | 扫掠检查执行帧 | 命中帧 | 命中次数 |
|---|---|---|---|---|
| multi_light（3 红绿灯 → 0.5m 虚拟墙） | 880 | 26 | **0** | **0** |
| city_comprehensive | 660 | （同样 0 命中） | **0** | **0** |

**结论：W3 理论成立、当前场景未复现。** 处理方式是把扫掠检查留成**常驻绊线**（每场景几十次、
开销可忽略），并在 debug JSON 里区分"没检查"和"检查了没命中"；一旦哪个场景出现命中，
再把它升级为 `traj.valid = 0`（那是行为变更，届时单独做 + 重生成 baseline）。
另需注意本检查用的是**当前**障碍物位置，静态障碍（红灯墙/施工区）语义与 Frenet 完全一致，
运动障碍差一个 2s 外推 —— 那是 W4 的范畴。

### 17.2 顺带挖出并修掉一个"dlopen 才炸"的链接缺陷 + 新 gate

做 W3 时踩了一个坑：新模块 `traj_safety.c` 加进了测试目标，**忘了加进 `planning_node` 目标**。
链接器不报错（共享库允许未定义符号），于是：

```
flow_launcher: symbol lookup error: build/lib/libplanning_node.so: undefined symbol: traj_swept_check
```

launcher 在 dlopen 阶段直接死掉 → 整个 pipeline 没起来 → evaluator 只给出一句
`run truncated: sample span 0.0s < 50%`，看起来像基础设施抖动（我一开始就是按抖动判的，
查了 launcher.stderr 才看到真因）。

新增 `ci/gates/plugin_symbol_check.py`（已进 CI 的 `integration-test`，紧跟 build node plugins）：

- 对每个 `lib*_node.so`：取未解析符号 U；减去 launcher 导出的符号（插件正常靠宿主解析）
  与系统/第三方前缀（libc/libm/libstdc++/cJSON/esmini…）
- 剩下的若在**任何项目产物**里被定义过（用完整符号表 `nm`，这样"只被静态链进测试
  可执行文件"的纯模块也能被认出来）→ 判定为"漏链"，报错并指出该查哪个 CMake 目标
- 自测：把 `traj_safety.c` 从 planning_node 目标摘掉复现原故障 → gate exit 1 并点名
  `libplanning_node.so: traj_swept_check`

**第二个受害者（既有的，非本次引入）**：gate 一上线就查出
`libimu_driver_node.so` 未解析 `imu_protocol_parse_line` / `imu_protocol_make_static`
—— 2026-09 那次「抽 imu_protocol 纯逻辑」把函数搬进新 .c 时同样漏加节点目标。
`imu_driver` 不在默认编排里（hw profile 用），所以一直没人发现：**任何真车/硬件 profile
一启动就会在 dlopen 处死**。已修（`imu_driver_node` 目标补上 `imu_protocol.c`）。

这条也把之前两次"看似基础设施抖动"的失败解释了：至少 12:39 那次 multi_light 的
`no topology samples collected` 与本次同源（真因要看 `launcher.stderr`，evaluator 的
报错信息会误导）。

---

## 18. 收尾：最终验证 + P1/P2 结算

### 18.1 最终全量验证（本 session 全部改动）

| 项 | 结果 |
|----|------|
| 顶层构建 / adas_nodes 构建 | ✅ 通过（无新增 warning） |
| ctest 顶层 | ✅ 29/29 |
| ctest adas_nodes | ✅ 7/7 |
| `test_adas_nodes_logic` | ✅ 46/46（新增 traj_swept 4 例） |
| 五个 gate（topic / msg-layout / plugin-symbol / zombie / scenarioctl） | ✅ 全 OK |
| `scenario_regression --baseline` | ✅ **8/8 全 PASS，regress 列全空** |

### 18.2 P1/P2 结算（相对 `HANDOFF_2026-09-21.md` §7）

| 原项 | 状态 |
|---|---|
| P1 perception_node 接 `sensor/lidar_points` 删 fakery | ✅ 完成（含两处契约修复） |
| P1 `world/buildings` OBB 射线投射 | ✅ 判定冗余不做（consumer LOS 等价覆盖，§11.5） |
| P1 抽出剩余模块纯逻辑 | ✅ 5/5（imu/pwm/sensor_model/slam/safety_control） |
| P1 规划 W1 车道内避障 | ⛔ **待你决策**（三选项见 §12） |
| P1 `ltv_mpc` 启用 | ✅ 以"充分回归 → 不达标"结案（§11.4，lane_change_traffic 3/3 碰撞） |
| P2 规划 W3 碰撞点测非扫掠 | ✅ 定性完成：理论成立、当前场景未复现 → 留常驻绊线（§17.1） |
| P2 规划 W4 无预测建模 | ⛔ 未做 —— 会改规划输入（行为变更 + baseline 重生成） |
| P2 规划 W5 FSM 死状态 | ⛔ 未做 —— **不是纯遥测**：`planning_node.cpp:1584` 消费 behavior 的 `target_speed` 当 command_speed，加进入转移会直接改纵向行为，必须同时设计退出条件 + 重生成 baseline |
| P2 `hw` profile 集成测试 | ⛔ 未做（需真实设备）；**顺带修掉一个会让 hw profile 起不来的漏链缺陷**（§17.2） |

本次额外产出（不在原计划内）：msg 线格式 gate、感知指标真实化（含两个度量伪影修复）、
插件符号 gate + imu_driver 漏链修复、W3 绊线。

### 18.3 下一个 session 的四个决策点

1. **W1（最该先定）**：参考线改车道中心 + `kd>0`（正解，动 baseline）／局部绕行（启发式）／
   判定为设计选择（L2 保守优先）。见 §12。
2. **W4/W5 要不要做**：两者都会改驾驶行为 → 必须重生成 baseline；W5 还要求先定义
   STOP/YIELD/EMERGENCY 的**退出条件**（现有转移表里它们只出不进，且只有 UTURN 一条出边）。
3. **sensor 模式切默认**：前置是量程/射线数策略（actors 多在 56~90m 而量程 60m，
   实测 recall 0.066 / coverage 0.502）。
4. **`msg_codegen` 是否 pack**：动 12 个类型的 ABI（录制回放/dashboard/baseline），
   现在有 gate 钉住现状，不急。
