{
  "name": "flowengine",
  "private": true,
  "scripts": {
    "vis:check": "node --import ./tests/support/three-preload.mjs tests/vis_module_load.test.mjs && npx --yes eslint -c tools/flowboard/eslint.config.mjs tools/flowboard/js/vis/ --format json | node -e \"let d='';process.stdin.on('data',c=>d+=c);process.stdin.on('end',()=>{const r=JSON.parse(d);let e=0,w=0;r.forEach(f=>f.messages.forEach(m=>{if(m.severity===2)e++;else w++;}));console.log('ESLint: '+e+' errors, '+w+' warnings');if(e>0){r.forEach(f=>f.messages.forEach(m=>console.error(f.filePath+':'+m.line+' '+m.message)));}process.exit(e?1:0);})\" && node --import ./tests/support/three-preload.mjs tests/vis_render_tick.test.mjs",
    "vis:check:invariant": "node --import ./tests/support/three-preload.mjs tests/vis_coord_property.test.mjs",
    "vis:check:grep": "node tests/vis_grep_enforce.mjs",
    "vis:check:junction": "node --import ./tests/support/three-real-preload.mjs tests/junction_markings.test.mjs",
    "vis:check:smoke": "node tests/run_vis_smoke.mjs",
    "vis:check:all": "npm run vis:check && npm run vis:check:invariant && npm run vis:check:grep && npm run vis:check:junction && npm run vis:check:smoke"
  }
}